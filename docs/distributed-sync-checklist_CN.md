# ???????????? + ???????

## 1. ????

??? `docs/distributed-sync-two-node-setup_CN.md` ???????

????????????????
`docs/distributed-sync-incident-matrix_CN.md` ?????

??????????? + ?? PostgreSQL + ?? Redis???????

- Node A: `http://127.0.0.1:18317`
- Node B: `http://127.0.0.1:28317`
- PostgreSQL: `127.0.0.1:15432`
- Redis: `127.0.0.1:16379`

?????

- compose ???`docs/docker-compose-distributed-sync-two-node.yaml`
- management key?`dev-mgmt-key`
- schema?`cliproxy_dist`

## 2. ??????

### 2.1 ???

- [ ] ??? Docker Desktop?? `docker compose` ???
- [ ] ??? `compose-data/node-a/pgstore/config/config.yaml`
- [ ] ??? `compose-data/node-a/pgstore/auths/`
- [ ] ??? `compose-data/node-b/pgstore/auths/`
- [ ] ????? `distributed-sync.enabled: true`
- [ ] ????? `distributed-sync.node-id: ""`
- [ ] ????? `remote-management.allow-remote: true`
- [ ] ????? `distributed-sync.redis.addr: "redis:6379"`

### 2.2 ???

- [ ] `postgres` ?????
- [ ] `redis` ?????
- [ ] `node-a` ?????
- [ ] `node-b` ?????
- [ ] ??????? `distributed sync manager started`
- [ ] PostgreSQL ???? `config_store`
- [ ] PostgreSQL ???? `auth_store`
- [ ] PostgreSQL ???? `sync_versions`
- [ ] PostgreSQL ???? `sync_outbox`
- [ ] `sync_versions` ????? `config`

### 2.3 ??????

- [ ] ?? A ? management API ???
- [ ] ?? B ? management API ???
- [ ] ??? A ?? `config.yaml` ??
- [ ] ???????????? A ??
- [ ] `sync_versions` ? `config.version` ???
- [ ] `sync_outbox` ??? `resource='config'` ??
- [ ] ?? B ???????????
- [ ] ?? B ?? `reload-from-store` ??

### 2.4 ?? auth ????

- [ ] ?? A ?? auth ??
- [ ] PostgreSQL ? `auth` ?????
- [ ] ?? B ?????? auth
- [ ] ?? B ?? `auth-files/reload-from-store` ??

### 2.5 ????

- [ ] ????????
- [ ] ????????
- [ ] ??????? `node-id` ??
- [ ] ??????? Redis ????

## 3. PowerShell ????

```powershell
$compose = ".\docs\docker-compose-distributed-sync-two-node.yaml"
$baseA = "http://127.0.0.1:18317"
$baseB = "http://127.0.0.1:28317"
$mgmtKey = "dev-mgmt-key"
$headers = @{ Authorization = "Bearer $mgmtKey" }
```

????? compose ?? `MANAGEMENT_PASSWORD`???? `$mgmtKey` ??????

## 4. ?????????

### 4.1 ??

```powershell
docker compose -f $compose up -d --build
```

### 4.2 ??????

```powershell
docker compose -f $compose ps
```

### 4.3 ??????

```powershell
docker compose -f $compose logs --tail=200 postgres redis node-a node-b
```

### 4.4 ?????

```powershell
docker compose -f $compose logs -f --tail=100 node-a node-b
```

????????????

- `distributed sync manager started`
- `config file changed, reloading`
- `failed to persist config change`
- `distributed sync: subscribe failed, using polling fallback only`
- `distributed sync: version gap detected`

## 5. Management API ????

### 5.1 ?? A ????

```powershell
Invoke-RestMethod -Headers $headers "$baseA/v0/management/config"
```

### 5.2 ?? B ????

```powershell
Invoke-RestMethod -Headers $headers "$baseB/v0/management/config"
```

### 5.3 ???? A ?? YAML

```powershell
curl.exe -sS `
  -H "Authorization: Bearer $mgmtKey" `
  "$baseA/v0/management/config.yaml" `
  -o .\node-a-config.yaml
```

### 5.4 ???? B ?? YAML

```powershell
curl.exe -sS `
  -H "Authorization: Bearer $mgmtKey" `
  "$baseB/v0/management/config.yaml" `
  -o .\node-b-config.yaml
```

## 6. PostgreSQL / Redis ????

### 6.1 PostgreSQL ???

```powershell
docker compose -f $compose exec postgres `
  psql -U cliproxy -d cliproxy -c "SELECT 1;"
```

### 6.2 ?? schema ???

```powershell
docker compose -f $compose exec postgres `
  psql -U cliproxy -d cliproxy -c "SELECT table_name FROM information_schema.tables WHERE table_schema = 'cliproxy_dist' ORDER BY table_name;"
```

???????

- `auth_store`
- `config_store`
- `sync_outbox`
- `sync_versions`

### 6.3 ?????

```powershell
docker compose -f $compose exec postgres `
  psql -U cliproxy -d cliproxy -c "SELECT resource, version, updated_at FROM cliproxy_dist.sync_versions ORDER BY resource;"
```

### 6.4 ?? outbox

```powershell
docker compose -f $compose exec postgres `
  psql -U cliproxy -d cliproxy -c "SELECT id, resource, version, operation, message, created_at FROM cliproxy_dist.sync_outbox ORDER BY id DESC LIMIT 20;"
```

### 6.5 Redis ???

```powershell
docker compose -f $compose exec redis redis-cli ping
```

?????

```text
PONG
```

## 7. ????????

### 7.1 ???? A ???????

```powershell
curl.exe -sS `
  -H "Authorization: Bearer $mgmtKey" `
  "$baseA/v0/management/config.yaml" `
  -o .\node-a-config.yaml
```

????

```yaml
request-retry: 3
```

???

```yaml
request-retry: 5
```

### 7.2 ???? A

```powershell
curl.exe -sS -X PUT `
  -H "Authorization: Bearer $mgmtKey" `
  -H "Content-Type: application/yaml" `
  --data-binary "@.\node-a-config.yaml" `
  "$baseA/v0/management/config.yaml"
```

?????

```json
{"ok":true,"changed":["config"]}
```

### 7.3 ??????

```powershell
Start-Sleep -Seconds 10
```

### 7.4 ?? PostgreSQL ????

```powershell
docker compose -f $compose exec postgres `
  psql -U cliproxy -d cliproxy -c "SELECT resource, version, updated_at FROM cliproxy_dist.sync_versions ORDER BY resource;"
```

### 7.5 ???? B ????

```powershell
curl.exe -sS `
  -H "Authorization: Bearer $mgmtKey" `
  "$baseB/v0/management/config.yaml" `
  -o .\node-b-config.yaml
```

?????????

```yaml
request-retry: 5
```

## 8. ????????

### 8.1 ?? B ??????

```powershell
curl.exe -sS -X POST `
  -H "Authorization: Bearer $mgmtKey" `
  "$baseB/v0/management/config.yaml/reload-from-store"
```

### 8.2 ?? B ???? auth

```powershell
curl.exe -sS -X POST `
  -H "Authorization: Bearer $mgmtKey" `
  "$baseB/v0/management/auth-files/reload-from-store"
```

## 9. ?? auth ????

### 9.1 ???? B auth ??

```powershell
curl.exe -sS `
  -H "Authorization: Bearer $mgmtKey" `
  "$baseB/v0/management/auth-files"
```

### 9.2 ???? A auth ??

```powershell
curl.exe -sS `
  -H "Authorization: Bearer $mgmtKey" `
  "$baseA/v0/management/auth-files"
```

## 10. ??????

### 10.1 ?? A PUT ???? `sync_versions` ???

???

- Node A ??
- PostgreSQL ????
- watcher ????

?????

```powershell
docker compose -f $compose logs --tail=200 node-a
```

### 10.2 `sync_versions` ??????? B ???

???

- Node B ??????
- Redis ????
- ??? `node-id` ????

?????

```powershell
docker compose -f $compose logs --tail=200 node-b
docker compose -f $compose exec redis redis-cli ping
```

### 10.3 ?????????????

?????

- PostgreSQL ??????
- ?? reload ?????
- ????? Redis ?????????

?????

- `distributed-sync.redis.addr`
- `distributed-sync.channel`
- `distributed-sync.node-id`
- ?? hostname

## 11. ??????

### 11.1 ??????

```powershell
docker compose -f $compose down
```

### 11.2 ??????

```powershell
docker compose -f $compose down -v
```

### 11.3 ????????

```powershell
Remove-Item .\node-a-config.yaml -ErrorAction SilentlyContinue
Remove-Item .\node-b-config.yaml -ErrorAction SilentlyContinue
```

## 12. ??????

??????????

1. ??? 4 ???????????????
2. ??? 5 ????????? management API ????
3. ??? 6 ???? PostgreSQL / Redis ?????
4. ???? 7 ??? 8 ??????????????

?????????????????????????

1. `docker compose -f $compose up -d --build`
2. `docker compose -f $compose logs --tail=200 node-a node-b`
3. `Invoke-RestMethod -Headers $headers "$baseA/v0/management/config"`
4. `Invoke-RestMethod -Headers $headers "$baseB/v0/management/config"`
5. ??????? A ??
6. ?? `sync_versions`
7. ???? B ????????
