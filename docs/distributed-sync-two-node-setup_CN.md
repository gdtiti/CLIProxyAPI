# ?????????????

## 1. ????

?????? Phase 1 ?????????????????????

- 2 ? CLIProxyAPI Plus ??
- 1 ??? PostgreSQL
- 1 ??? Redis

??????????????

1. ?? A ??????? auth
2. PostgreSQL ?? `config_store` / `auth_store` ??
3. `sync_versions` / `sync_outbox` ????
4. Redis Pub/Sub ??????
5. ?? B ???? `reload-from-store + runtime reload`

??????????

- PostgreSQL ??????
- Redis ?????????????
- ???????????????????? 200 ?????????
- ????????? PostgreSQL version ????

## 2. ????

### 2.1 ????

?????????

- ???? / ?? VM
- ???????????

?????

- ????????? `port`
- `distributed-sync.node-id` ??????????
- ??????????????????????????? ID ??

### 2.2 ?????

????????????????????? PG store ?????

???

- ?? `config_store` ????? `port`
- ?? `distributed-sync.node-id` ???????? `hostname-port`
- ??????????????????????????????????

### 2.3 ????

```text
                    +-------------------+
                    |      Redis        |
                    |   Pub/Sub only    |
                    +---------+---------+
                              |
          publish / subscribe | 
                              |
+----------------+            |            +----------------+
|    Node A      |------------+------------|    Node B      |
| local spool A  |                         | local spool B  |
| watcher/runtime|                         | watcher/runtime|
+--------+-------+                         +--------+-------+
         \                                          /
          \                                        /
           \                                      /
            +------------------------------------+
            |         PostgreSQL                 |
            | config_store / auth_store          |
            | sync_versions / sync_outbox        |
            +------------------------------------+
```

## 3. ?????????

????????????

### 3.1 ????????

?? PG ?? `config_store` ???????????

?????????????????????

- `port`
- `host`
- `distributed-sync.channel`
- `distributed-sync.poll-interval-seconds`
- ????? `PUT /v0/management/config.yaml` ??????

???

- ????????????? `port`
- ????????????? `port`
- ??????????????? `port`

### 3.2 `distributed-sync.node-id` ??????

????????? `distributed-sync.node-id` ????????

?????????

```yaml
distributed-sync:
  enabled: true
  node-id: ""
```

?????????

- `<hostname>-<port>`??? `port > 0`
- `<hostname>`???????

???????????????????????????

### 3.3 ?????????

`PUT /v0/management/config.yaml` ?????

1. ???? A ????????
2. ????? A ???????
3. ? watcher ???? `PersistConfig`
4. PostgreSQL ???????? distributed version/outbox
5. ?? Redis ????????

???????? management API ? 200 ????? B ??????????

## 4. ????

### 4.1 ????

????????????????????????

### 4.2 PostgreSQL

???????? PostgreSQL?

??????????

- `config_store`
- `auth_store`
- `sync_versions`
- `sync_outbox`

????? `PGSTORE_SCHEMA`????????? schema ??

### 4.3 Redis

???? Redis?

????????

- Pub/Sub channel

???? channel?

- `cliproxy:distributed-sync:events`

### 4.4 ??????

??????????????????

????????

- `PGSTORE_LOCAL_PATH`

?????????

- `<PGSTORE_LOCAL_PATH>/pgstore/config/config.yaml`
- `<PGSTORE_LOCAL_PATH>/pgstore/auths/`

???????????? `PGSTORE_LOCAL_PATH`?

## 5. ???????????

## 5.1 ????

????????????????

- ??????
- API keys / provider ??
- `remote-management` ??
- `distributed-sync.enabled`
- `distributed-sync.channel`
- `distributed-sync.poll-interval-seconds`
- `distributed-sync.redis.*`

### 5.2 ??????

??????????????

- `PGSTORE_LOCAL_PATH`
- `MANAGEMENT_PASSWORD`
- ??? / ???
- ??????????????

### 5.3 ?? `auth-dir`

??? `PGSTORE_*` ??????? `cfg.AuthDir` ??? Postgres store ????????

?????

- ?????? `auth-dir` ????????
- ?????? `<PGSTORE_LOCAL_PATH>/pgstore/auths`

????? `auth-dir` ?????????

## 6. ??????

???????????????????

???

- `node-id` ???
- `port` ??????? `8317`
- ????? Docker ??????????????`remote-management.allow-remote`
  ??? `true`
- ?????????????????????

```yaml
host: '0.0.0.0'
port: 8317

remote-management:
  allow-remote: true
  secret-key: ''
  disable-control-panel: false

auth-dir: './ignored-when-pgstore-enabled'

debug: false
logging-to-file: false
usage-statistics-enabled: false

distributed-sync:
  enabled: true
  node-id: ""
  channel: "cliproxy:distributed-sync:events"
  poll-interval-seconds: 15
  redis:
    addr: "127.0.0.1:6379"
    username: ""
    password: ""
    db: 0

request-retry: 3
force-model-prefix: false
```

??????? Docker ??????????????????????????
`remote-management.allow-remote` ?? `false`?

## 7. ?????????

?? Postgres store ???? YAML ??????????????????

????????????

- `PGSTORE_DSN`
- `PGSTORE_SCHEMA`
- `PGSTORE_LOCAL_PATH`
- `MANAGEMENT_PASSWORD`

???

- `PGSTORE_DSN`?PostgreSQL ???
- `PGSTORE_SCHEMA`??????? schema
- `PGSTORE_LOCAL_PATH`????????????
- `MANAGEMENT_PASSWORD`?management API ?????????????????????? `remote-management.secret-key`

## 8. ??????

## 8.1 ????????

?? PostgreSQL ???? `config_store.id='config'` ?????

- ??????? `<PGSTORE_LOCAL_PATH>/pgstore/config/config.yaml`
- ????????????????? `config.example.yaml` ??????

????????

- ???? A ??????????
- ?? B ????

## 8.2 ?? A ??????

? PowerShell ???

```powershell
$nodeARoot = "J:\cliproxy-lab\node-a"
New-Item -ItemType Directory -Force "$nodeARoot\pgstore\config" | Out-Null
New-Item -ItemType Directory -Force "$nodeARoot\pgstore\auths" | Out-Null
```

?????????????

```text
J:\cliproxy-lab\node-a\pgstore\config\config.yaml
```

?? A ????????????? PostgreSQL?

?? PostgreSQL ??????????

- ??????????????
- ??????? PostgreSQL ??????

## 9. ???????

## 9.1 ?? A

PowerShell ???

```powershell
$env:PGSTORE_DSN = "postgres://user:password@127.0.0.1:5432/cliproxy?sslmode=disable"
$env:PGSTORE_SCHEMA = "cliproxy_dist"
$env:PGSTORE_LOCAL_PATH = "J:\cliproxy-lab\node-a"
$env:MANAGEMENT_PASSWORD = "dev-mgmt-key"

go run ./cmd/server
```

?????????

- ??? PostgreSQL ??????
- ??? `distributed sync manager started`

## 9.2 ?? B

```powershell
$env:PGSTORE_DSN = "postgres://user:password@127.0.0.1:5432/cliproxy?sslmode=disable"
$env:PGSTORE_SCHEMA = "cliproxy_dist"
$env:PGSTORE_LOCAL_PATH = "J:\cliproxy-lab\node-b"
$env:MANAGEMENT_PASSWORD = "dev-mgmt-key"

go run ./cmd/server
```

???

- `PGSTORE_DSN` ? `PGSTORE_SCHEMA` ??? A ??
- `PGSTORE_LOCAL_PATH` ??? A ??

## 9.3 ???????

??????????????????????

?????? `8317` ??????????????

- Node A?`18317 -> 8317`
- Node B?`28317 -> 8317`

?????????

- ?????????? `port: 8317`
- ?????????????
- ?? hostname ?????? `node-id` ??????

### 9.4 ????? docker-compose ??

??????????????????

- `docs/docker-compose-distributed-sync-two-node.yaml`
- `docs/distributed-sync-config.shared.yaml`
- `docs/distributed-sync-checklist_CN.md`
- `docs/distributed-sync-incident-matrix_CN.md`

???????

- PostgreSQL ?????? `15432`
- Redis ?????? `16379`
- Node A ?????? `18317`
- Node B ?????? `28317`
- ????????? `PGSTORE_DSN`
- ????????? `PGSTORE_SCHEMA`
- ????????? `PGSTORE_LOCAL_PATH`
- ????????? `MANAGEMENT_PASSWORD`
- ?????????? hostname

??????

```powershell
New-Item -ItemType Directory -Force ".\compose-data\node-a\pgstore\config" | Out-Null
New-Item -ItemType Directory -Force ".\compose-data\node-a\pgstore\auths" | Out-Null
New-Item -ItemType Directory -Force ".\compose-data\node-b\pgstore\auths" | Out-Null
Copy-Item ".\docs\distributed-sync-config.shared.yaml" `
  ".\compose-data\node-a\pgstore\config\config.yaml"
```

???

- ???? A ??????
- ?? B ??????? PostgreSQL ???????? `config.yaml`
- ????? compose ?? `MANAGEMENT_PASSWORD`????????????
- ??????????? `remote-management.allow-remote` ?? `true`????
  ?????????????????????? localhost
- ??????????????? `docs/distributed-sync-checklist_CN.md` ???
  ??
- ??????????????? `docs/distributed-sync-incident-matrix_CN.md`
  ????? -> ????? -> ?????

???

```powershell
docker compose -f .\docs\docker-compose-distributed-sync-two-node.yaml up -d --build
```

?????

```powershell
docker compose -f .\docs\docker-compose-distributed-sync-two-node.yaml logs -f node-a node-b
```

???

```powershell
docker compose -f .\docs\docker-compose-distributed-sync-two-node.yaml down
```

????? PostgreSQL ???????

```powershell
docker compose -f .\docs\docker-compose-distributed-sync-two-node.yaml down -v
```

## 10. ???????

?? A ??? B ??????????????

### 10.1 Management API ????

????????????????????`remote-management.allow-remote: false`
?????

????? Docker ??????????????????????? localhost?
????? `remote-management.allow-remote` ?? `true`?

?????

- `Authorization: Bearer <key>`
- `X-Management-Key: <key>`

PowerShell ???

```powershell
$headers = @{ Authorization = "Bearer dev-mgmt-key" }
Invoke-RestMethod -Headers $headers http://127.0.0.1:8317/v0/management/config
```

????????????

- ?? A?`http://127.0.0.1:18317`
- ?? B?`http://127.0.0.1:28317`

### 10.2 PostgreSQL ??????

? `cliproxy_dist` schema ???

```sql
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'cliproxy_dist'
ORDER BY table_name;
```

???????

- `auth_store`
- `config_store`
- `sync_outbox`
- `sync_versions`

### 10.3 ????????

```sql
SELECT resource, version, updated_at
FROM cliproxy_dist.sync_versions
ORDER BY resource;
```

??????? bootstrap???????

- `config`

????? auth ????????

- `auth`

## 11. ????????

????????????????????

## 11.1 ???????????

????????

- `port`
- `host`
- `distributed-sync.node-id`

????????????

- `request-retry`
- `force-model-prefix`
- `usage-statistics-enabled`
- `debug`

??? `request-retry` ???

## 11.2 ??? A ???? YAML

```powershell
curl.exe -sS `
  -H "Authorization: Bearer dev-mgmt-key" `
  http://127.0.0.1:8317/v0/management/config.yaml `
  -o .\node-a-config.yaml
```

????

```yaml
request-retry: 3
```

???

```yaml
request-retry: 5
```

## 11.3 ????? YAML ???? A

```powershell
curl.exe -sS -X PUT `
  -H "Authorization: Bearer dev-mgmt-key" `
  -H "Content-Type: application/yaml" `
  --data-binary "@.\node-a-config.yaml" `
  http://127.0.0.1:8317/v0/management/config.yaml
```

?????

```json
{"ok":true,"changed":["config"]}
```

?????

- ??????? A ???????????????
- ?????? PG ??? B ??????

## 11.4 ?? watcher ?????

?????

- 3 ? 10 ?

???? `distributed-sync.poll-interval-seconds` ?? 15 ????????????

- watcher debounce + PostgreSQL ?? + Redis ?? + ?? B ??
- ? Redis ?????????? poll interval

## 11.5 ?? PostgreSQL ??? outbox

```sql
SELECT resource, version, updated_at
FROM cliproxy_dist.sync_versions
ORDER BY resource;
```

```sql
SELECT id, resource, version, operation, message, created_at
FROM cliproxy_dist.sync_outbox
ORDER BY id DESC
LIMIT 20;
```

?????

- `config` ? `version` ??
- `sync_outbox` ???? `resource='config'`
- `operation='upsert'`

## 11.6 ???? B ????

??? B ???????

```powershell
curl.exe -sS `
  -H "Authorization: Bearer dev-mgmt-key" `
  http://127.0.0.1:8317/v0/management/config.yaml `
  -o .\node-b-config.yaml
```

?????????

```yaml
request-retry: 5
```

??????????????? B URL ???

- `http://127.0.0.1:28317/v0/management/config.yaml`

## 12. ??????

?????????????????store ???????????????????

### 12.1 ?? B ??????

```powershell
curl.exe -sS -X POST `
  -H "Authorization: Bearer dev-mgmt-key" `
  http://127.0.0.1:8317/v0/management/config.yaml/reload-from-store
```

???????

```json
{
  "ok": true,
  "source": "store",
  "target": "config",
  "store": "postgres",
  "changed": true
}
```

????????????????????????

- Redis ??????
- Redis ???????
- ???? `node-id` ??

### 12.2 ?? B ???? auth

```powershell
curl.exe -sS -X POST `
  -H "Authorization: Bearer dev-mgmt-key" `
  http://127.0.0.1:8317/v0/management/auth-files/reload-from-store
```

??????????? auth ??????

## 13. ??? auth ??

????????? auth ?????????????? auth JSON????????????????

?????

1. ??? A ?? `POST /v0/management/auth-files`
2. ?? PostgreSQL / Redis / ?? B ????
3. ??? B ?? `GET /v0/management/auth-files` ??????? auth

???

- auth ????????? B ?????
- ???????????

## 14. ??????

????????????

### 14.1 ?? A ?????? `sync_versions` ???

?????

- watcher ????? config ????
- watcher `PersistConfig` ??
- PostgreSQL ????

????

- ?? A ??????? `config file changed, reloading`
- ???? `failed to persist config change`

### 14.2 `sync_versions` ?????? B ???

?????

- Redis ?????
- ?? B ????
- ??? `node-id` ??????????????

????

- ?? A / B ????? `distributed sync manager started`
- ??? `distributed sync: subscribe failed, using polling fallback only`
- ??? `distributed sync: version gap detected`

### 14.3 ?? `reload-from-store` ?????????

???

- PostgreSQL ?????????????
- ????? Redis ???????

?????

- `distributed-sync.enabled`
- `distributed-sync.redis.addr`
- `distributed-sync.channel`
- `distributed-sync.node-id`
- ??? / ?? hostname ????

### 14.4 ?? B ???????????

?????

- Redis ???????? poll reconcile ???
- `distributed-sync.poll-interval-seconds` ????

????????

```yaml
distributed-sync:
  poll-interval-seconds: 5
```

?????????????????????????

## 15. ??????????

????????? Phase 1 ?????????????

1. ?????????????? `distributed sync manager started`
2. ?? A ????????PostgreSQL `sync_versions` / `sync_outbox` ???
3. ?? B ??????????????
4. ??????????? B ???? `reload-from-store` ????

## 16. ???????????

???????????? Phase 1?

??????

- 3h / 6h / 24h / ? / ?????????
- ??? patch ??
- ????????
- ????? outbox ????

??????????????????????? Phase 2 ??????????????
