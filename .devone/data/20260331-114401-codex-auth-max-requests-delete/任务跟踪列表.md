# 任务跟踪列表

## 文档元信息

- 任务名称: codex-auth-max-requests-delete
- 任务目录: .devone/data/20260331-114401-codex-auth-max-requests-delete
- 创建时间: 20260331-114401
- 工作流档位: 极简 (devone-mini)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- Worktree 目录:
- Worktree 分支:
- 开发端口:
- 当前阶段: acceptance
- 当前状态: accepted

## 执行策略

- 当前选择:
  - `full`: 全量执行并跟踪必备任务与可选任务
  - `required-only`: 只执行必备任务；可选任务保留记录但默认不推进
- 本次实际模式: required-only
- 工作流日志: `.devone/wiki/logs/devone-workflow-log.md`（只用于人工检查，不作为平时知识读取入口）
- 结构化回写提示:
  - 更新阶段、波次、聚焦任务时，优先使用 `update-status`
  - 更新单个任务块的类型、状态、前置条件、产出、验证或备注时，优先使用 `update-task-block`
  - 模式切换时，优先使用 `sync-mode`

## 状态枚举

- `pending`: 尚未开始
- `in_progress`: 正在执行
- `blocked`: 有明确阻塞
- `done`: 已完成且已有证据
- `deferred`: 已确认延期
- `skipped_optional`: 因执行模式或用户选择而跳过的可选任务
- `rejected`: 经验收失败

## 任务包状态枚举

- `in_progress`: 当前任务包仍在推进
- `blocked`: 当前任务包存在明确阻塞
- `done`: 实现已完成，待正式验收
- `accepted`: 验收通过
- `conditionally_accepted`: 有条件通过
- `rejected`: 验收未通过

## 必备任务

### R1. 锁定问题定义与执行模式

- 类型: 必备任务
- 状态: done
- 前置条件:
  - 已确认项目根目录与执行模式假设
- 产出:
  - 需求说明.md
- 验证:
  - 问题定义、目标、范围已回写
- 备注: 按 devone-mini + required-only + classic + single 执行

### R2. 形成可批准设计与阶段计划

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R1 已完成，相关代码与 wiki 已调研
- 产出:
  - 设计书.md; 单元测试设计文档.md
- 验证:
  - 方案、阶段计划、测试入口已回写
- 备注: 采用 auth-maintenance + runtime hook 复用方案

### R2.5. 创建 worktree 分支与开发端口

- 类型: 必备任务
- 状态: pending
- 前置条件:
  - R2 已完成
  - 设计已批准
- 产出:
  - `.devone/worktree/<任务id>/` 目录可用
  - 独立 git 分支已创建/已绑定到 worktree
  - 端口已分配并写回文档元信息
- 验证:
  - `worktree-status` 显示 worktree 已创建且状态清晰
  - `Worktree 目录 / Worktree 分支 / 开发端口` 已写入 `## 文档元信息`
- 备注:

### R3. 完成最小可验证实现

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R2 已完成，目标文件已定位
- 产出:
  - internal/config/*; sdk/cliproxy/auth/runtime_persistence.go; internal/api/handlers/management/auth_files_runtime_hook*; config.example.yaml; runtime-auth-maintenance.md; 技术说明.md
- 验证:
  - 配置/运行态测试通过，hook 精确测试通过
- 备注: management 包存在与本轮无关的既有失败测试

### R4. 完成验收门禁核对

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R3 已完成
  - 本轮 fresh evidence 已收集
- 产出:
  - 需求映射
  - 门禁结论
- 验证:
  - `REQ-001` 已由配置结构、配置示例与配置测试覆盖
  - `REQ-002` 已由 management hook 正反向测试覆盖
  - acceptance audit 通过；completion audit 在 R4 回写前唯一阻塞项为 R4 自身状态
- 备注: 结论为 `accepted`；`TestListAuthFiles_ExposesQuotaWindowFieldsAfterExecutionFailure` 为仓库既有失败，与本轮需求无直接关系

### R4.5. 合并回主线并清理 worktree

- 类型: 必备任务
- 状态: pending
- 前置条件:
  - R4 已完成且验收结论为 `accepted` / `conditionally accepted`
  - 用户明确确认允许合并与清理
- 产出:
  - worktree 分支已合并回目标主线分支
  - worktree 目录已移除
  - （可选）worktree 分支已删除
- 验证:
  - `worktree-merge --confirm` 执行成功
  - `worktree-remove --confirm` 执行成功
- 备注:

## 可选任务

### O1. 扩展优化与非关键增强

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 必备任务主链稳定
  - 用户允许扩展范围
- 产出: 增强项或体验优化
- 验证: 不影响必备链路
- 备注:

### O2. 全量回归或更深测试覆盖

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 核心修复已通过
  - 测试环境可用
- 产出:
  - 更广回归结果
  - 补充测试用例
- 验证: 回归结果已记录到 `技术说明.md` / `单元测试设计文档.md`
- 备注:

## 当前波次

- 当前波次: Wave 1
- 当前聚焦任务:
- 不在本波次的任务:

## 变更记录

- [20260331-114401] 初始化任务文档包，执行模式为 `required-only`
- [20260331-115146] 任务块=R1；状态=done；前置条件=1；产出=1；验证=1；备注=已更新
- [20260331-115146] 任务块=R2；状态=done；前置条件=1；产出=1；验证=1；备注=已更新
- [20260331-120004] 任务块=R3；状态=done；前置条件=1；产出=1；验证=1；备注=已更新
- [20260331-123900] 任务块=R4；状态=done；前置条件=1；产出=1；验证=3；备注=accepted，既有失败测试已单独复现并标记为非本轮阻塞
