# 相关知识

## 文档元信息

- 任务名称: codex-auth-max-requests-delete
- 任务目录: .devone/data/20260331-114401-codex-auth-max-requests-delete
- 创建时间: 20260331-114401
- 工作流档位: 极简 (devone-mini)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 已知事实

- 现有实现:
  - `auth-runtime` 已负责 `401` 连续失败阈值删除。
  - `auth-maintenance` 已负责 `usage_limit_reached` 禁用和扩展删除策略。
- 相关文档:
  - `.devone/wiki/projects/runtime-auth-maintenance.md`
- 历史约束:
  - 不引入静默回退，不新增第二套删除通道。

## 术语与定义

- 文件型 `codex` auth:
  - 有本地 JSON 文件承载、可由 management handler 删除的 `codex` provider auth。
- 累计请求次数:
  - 单个 auth 在运行态 `OnResult` 中累加的已完成请求数。

## 调研结论

- 结论 1:
  - 最小改动入口是 `internal/api/handlers/management/auth_files_runtime_hook.go`，因为现有 auth 删除/禁用都已在这里闭环。
- 结论 2:
  - 最合适的持久化位置是 `sdk/cliproxy/auth/runtime_persistence.go`，这样累计计数能跟现有运行态状态一起跨重启恢复。

## 交接差异矩阵

- 产品事实源:
  - 用户要求“每个 codexcli 认证文件可设置可使用次数，达到阈值后自动删除”。
- 知识事实源:
  - 运行态 auth 维护 wiki。
- 实现事实源:
  - `internal/config/config.go`
  - `internal/api/handlers/management/auth_files_runtime_hook.go`
  - `sdk/cliproxy/auth/runtime_persistence.go`
- 8 维摘要:
  - 当前实现已具备配置层、删除链路、store 持久化与测试入口，只缺累计请求阈值。

## 风险假设

- 假设:
  - “账号请求累计次数”按所有已完成请求理解是正确的。
- 验证方式:
  - 用 success 场景测试阈值触发删除；用阈值 `0` 测试默认关闭。

## 知识升级候选

- 可升级为正式 wiki 的知识:
  - `auth-maintenance.codex-max-request-count` 字段说明与使用建议。
- 仍停留在任务级草稿的知识:
  - 既有失败测试与本轮无关的排查结论。
- 升级前缺口:
  - 无

## 待补证据

- 如需更强证据，可追加一次带真实 auth 文件的手工集成验证。
