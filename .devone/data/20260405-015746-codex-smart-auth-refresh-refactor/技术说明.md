# 技术说明

## 文档元信息

- 任务名称: codex-smart-auth-refresh-refactor
- 任务目录: .devone/data/20260405-015746-codex-smart-auth-refresh-refactor
- 创建时间: 20260405-015746
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 实际改动摘要

- 本轮改动:
  - 在 `sdk/cliproxy/auth/types.go` 的 `Auth` 中新增运行态热度字段：`LastUsedAt`、`WarmUntil`、`ResidentUntil`，并增加 `AllowsBackgroundRefresh(now)` 统一判断入口。
  - 在 `sdk/cliproxy/auth/conductor.go` 中为成功请求增加热度写入逻辑：成功调用 `MarkResult()` 后刷新 `LastUsedAt`，并为 Codex 认证写入默认 `1h` 的 warm window。
  - 在 `sdk/cliproxy/auth/conductor.go` 的 `shouldRefresh()` 中对 Codex 引入 warm/resident gate；cold auth 即使命中 `RefreshLead=5d` 也不会参与后台 refresh。
  - 在 `preserveRuntimeAuthState()` 中保留热度字段，避免 refresh/update 后把 warm/resident 状态意外清空。
  - 新增 `sdk/cliproxy/auth/conductor_background_refresh_test.go`，覆盖 cold/warm/resident/disabled/update-preserve/mark-success 等关键行为。
- 涉及文件/模块:
  - `sdk/cliproxy/auth/types.go`
  - `sdk/cliproxy/auth/conductor.go`
  - `sdk/cliproxy/auth/conductor_background_refresh_test.go`
- 未改动但已核对:
  - `sdk/auth/codex.go`
  - `sdk/cliproxy/auth/simhash_selector.go`
  - `internal/runtime/executor/codex_executor.go`
- 结构化回写提示:
  - 更新固定章节时，优先使用 `update-doc-section`
  - 更新 `## Wave 执行记录` 下的单个 `### Wave N` 时，优先使用 `append-wave-record`
  - 更新 `## 并行批次记录` 下的单个 `### Batch N` 时，优先使用 `append-parallel-batch`

## Wave 执行记录

### Wave 1

- 目标:
  - 交付最小可验证的 Codex warm-gated auto-refresh，先止住 15 分钟近似全刷。
- 改动:
  - 新增通用运行态热度字段。
  - 成功请求后写入 warm window。
  - Codex 后台 refresh 改为仅 warm/resident auth 可进入。
  - 补齐单元测试。
- 结果:
  - `go test ./sdk/cliproxy/auth/...` 通过。
  - cold Codex auth 不再因 lead 逻辑进入后台 refresh；warm/resident auth 仍可 refresh。

### Closeout

- 目标: 对账共享 worktree、提交实现并判断 merge readiness

- 改动:
  - 在共享 worktree 提交 Codex warm/resident refresh + SimHash resident bridge 实现，提交为 cf7831ba5823e921f5ae322660110005531ccd1e。
  - 重新执行 go test ./sdk/cliproxy/auth/...，结果为 ok（cached）。
  - 尝试 push origin/devone/20260405-015746-codex-smart-auth-refresh-refactor，收到 403 Permission denied。
  - 执行 worktree-merge --into main dry-run，被主工作区 tracked files 未清理拦截。

- 结果:
  - 共享 worktree 当前 clean，closeout 结论为 blocked，cleanup 维持 kept。
  - 后续必须先清理主工作区并解决 origin 推送权限，才能继续固定 merge-back。

### CI Follow-up

- 目标: 补齐 devone 分支遗漏的 CI/build 修复，消除 buildx 旧命令失败

- 改动:
  - 在共享 worktree 将 c245f226 cherry-pick 到 devone/20260405-015746-codex-smart-auth-refresh-refactor，带入 Dockerfile 的 -mod=mod、docker-branch 的 models catalog 刷新、pr-test-build 的 -mod=mod

- 结果:
  - 生成新提交 89c2c63d，并已 push 到 gdtiti/devone/20260405-015746-codex-smart-auth-refresh-refactor；远端分支不再停留在旧的 cf7831ba

### Mainline Mergeback

- 目标: 将已验收的 devone 分支按固定 closeout 路径合并回远端主线 gdtiti/main

- 改动:
  - 在临时干净集成 worktree integration/20260406-gdtiti-main-mergeback 基于 gdtiti/main 做 merge dry-run、跑 sdk/cliproxy/auth 定向测试、提交 merge commit 59e100a8

- 结果:
  - 已 push gdtiti/main 到 59e100a8；目标实现与 CI 修复均进入远端主线；临时集成 worktree 保留，未做 cleanup

## 并行批次记录

- 使用提示:
  - 当前 wave 若使用子代理并行，优先把边界、写集、验证目标与汇总结论落到这里
  - 单个批次卡片优先通过 `append-parallel-batch` 回写

### Batch 1

- batch_type:
  - single-writer
- task_ids:
  - R3
- write_set:
  - `sdk/cliproxy/auth/types.go`
  - `sdk/cliproxy/auth/conductor.go`
  - `sdk/cliproxy/auth/conductor_background_refresh_test.go`
- verify_target:
  - Codex auto-refresh 只对 warm/resident auth 生效
- forbidden_actions:
  - 不修改主工作区实现代码
  - 不引入全局 provider 状态机重构
- single_writer:
  - 主代理
- findings:
  - 现有 refresh lead registry 在 `sdk/cliproxy/auth` 单包测试中不会自动初始化，因此测试使用了最小 runtime stub 来提供 `RefreshLead()`。
- results:
  - 最小闭环已落地，且未触及配额禁用/恢复链路。

## 验证记录

- 命令:
  - `& 'C:/Program Files/Go/bin/go.exe' test ./sdk/cliproxy/auth/...`
  - `& 'C:/Program Files/Go/bin/go.exe' test ./internal/api/handlers/management/... ./sdk/cliproxy/auth/...`
- 结果:
  - 第一条命令通过。
  - 第二条命令未执行完成，Go 提示 `updates to go.mod needed; to update it: go mod tidy`。
- 说明:
  - 本轮未执行 `go mod tidy`，避免把与本次改动无关的依赖整理混入交付；因此 acceptance 主要基于 `sdk/cliproxy/auth` 包的 fresh test evidence。

## 风险、阻塞与未验证项

- 风险:
  - warm window 当前为默认 `1h` 常量，只支持通过 auth metadata/attributes 做局部覆盖，尚未升格为全局配置。
  - 热度字段当前仅由“真实成功请求”驱动，虚拟池 admission / resident 桥接仍待后续波次。
- 阻塞:
  - 无实现阻塞；仅存在更广包级测试受 `go mod tidy` 要求影响。
- 未验证项:
  - 未做跨包全量回归。
  - 未验证虚拟池常驻信号桥接，因为本轮未落地该增强。

## 验收准备

- 可交给 `devone-acceptance` 的证据:
  - 代码 diff：`sdk/cliproxy/auth/types.go`、`sdk/cliproxy/auth/conductor.go`
  - fresh tests：`go test ./sdk/cliproxy/auth/...`
  - 任务包中的需求/设计/测试映射
- 仍需补充的证据:
  - 若要给出更强 acceptance，可在后续消化 `go mod tidy` 问题后补跑更广包级回归

## 验收门禁结论

- 交叉验证:
  - 主证据：`go test -count=1 -run 'TestManager_(ShouldRefresh_CodexColdAuthSkipsBackgroundRefresh|ShouldRefresh_CodexWarmAuthAllowsBackgroundRefresh|ShouldRefresh_CodexResidentAuthAllowsBackgroundRefresh|ShouldRefresh_CodexDisabledAuthStillSkipsBackgroundRefresh|MarkResult_SuccessWarmsCodexAuth|Update_PreservesBackgroundRefreshHeat)$' -v ./sdk/cliproxy/auth`
  - 辅证据：代码 diff + wiki/任务包一致性回写
  - 结果：一致
- 门禁结果:
  - Requirements Mapping: pass
  - Stub/Mock/Fallback Detection: pass
  - Real Data: pass（逻辑层范围内，走真实 `Manager/Auth` 代码路径与真实 `expired` 字段格式）
  - Wiring: pass
  - Cold Start: pass（`-count=1` 强制非缓存测试）
  - Regression and Original-Symptom Proof: pass
  - Runtime Verification: pass
  - User-Observable Acceptance: pass（本轮为运行态策略改动，用户可观察面体现在 refresh 选择语义）
  - Failure Visibility: pass
  - Critical Source Guardrails: not_applicable（仓库未发现对应 guardrail 文件）
- 初步结论:
  - accepted（仅限本轮代码与任务包验收；R4.5 的 merge/cleanup 仍待用户显式确认）
