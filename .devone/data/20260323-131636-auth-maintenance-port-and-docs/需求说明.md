# 需求说明

## 文档元信息

- 任务名称: auth-maintenance-port-and-docs
- 任务目录: .devone/data/20260323-131636-auth-maintenance-port-and-docs
- 创建时间: 20260323-131636
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 问题定义

- 当前问题:
  - 当前项目只有 management 层的 `AuthRuntimeMaintenanceHook`，缺少参考仓库 `AuthMaintenance` 的后台维护队列、去重与 watcher 自抑制能力。
  - 运行态 `coreManager` 请求路径对 `401`、`usage_limit_reached` 的处理不完整，内部写删 auth 文件时 watcher 也缺少自抑制，存在重复处理和状态抖动风险。
- 影响对象:
  - `sdk/cliproxy/service.go` 维护队列与运行态 auth 管理
  - `internal/watcher/*` 文件监听与增量重载
  - `internal/config/config.go` 配置结构
  - `.devone/wiki/` 与任务包文档
- 触发背景:
  - 用户要求参考 `J:\_Dev\_me\_cpa\CLIProxyAPI-Arron196` 移植 `AuthMaintenance`，同时明确不能破坏当前已经落地的行为。
- 为什么现在处理:
  - 当前项目已经有 `usage_limit_reached -> disable auth` 语义，需要在引入后台维护能力时把这条语义固定下来，避免被参考仓库的 `429 -> delete` 策略回退。

## 目标与成功标准

- 目标 1:
  - 为当前项目补齐 `AuthMaintenance` 的后台维护基础设施，包括配置、队列、去重、节流、扫描与 watcher 稳定性增强。
- 目标 2:
  - 保持并扩展当前行为边界：`401` 连续失败可删除；`codex usage_limit_reached` 只能禁用；普通 `codex 429` 不得被误删或误禁用。
- 目标 3:
  - 更新 DevOne 知识库与任务文档，补齐本次和此前相关改动的使用方式、配置方法与关系说明。

## 范围边界

### 本次必须完成

- 在 `Service/coreManager` 路径接入 `AuthMaintenance` 钩子与后台处理
- 在 watcher 中移植内部写删文件的自抑制与写入去抖
- 增加 `auth-maintenance` 配置结构与默认值整理
- 补充并运行关键测试
- 初始化并更新 `.devone/wiki/` 与任务包文档

### 本次明确不做

- 不回退已有 management API 行为
- 不把 generic `429` 一律升级成删除策略
- 不删除用户现有 auth/runtime 配置项
- 不做与本次维护逻辑无关的重构

## 验收真相陈述

- [REQ-001] 当前项目新增 `AuthMaintenance` 后，`codex usage_limit_reached` 仍然只会禁用 auth，不会删除 auth 文件。
- [REQ-002] 当前项目新增 `AuthMaintenance` 后，generic `codex 429` 不会误触发禁用或删除。
- [REQ-003] 当前项目支持参考仓库中的维护队列、路径去重、in-flight 去重、backlog 节流与 watcher 自抑制能力。
- [REQ-004] 知识库与任务文档已更新，能够说明本次和此前相关改动的配置方法、使用方式与相互关系。

## 约束与依赖

- 技术约束:
  - 不能破坏现有 `AuthRuntime` 及 management hook 逻辑。
  - Windows PowerShell 读写文档必须显式 UTF-8。
  - 代码编辑使用 `apply_patch`，避免破坏用户已有改动。
- 业务约束:
  - `usage_limit_reached -> disable auth` 是当前项目已确认行为，不允许回退成删除。
  - 文档必须覆盖“之前的改动配置方法也要更新”。
- 外部依赖:
  - 参考实现来源：`CLIProxyAPI-Arron196`
  - 当前项目测试框架与现有 `coreauth` / watcher / management 代码

## 风险与待确认项

- 风险:
  - watcher 自抑制与当前项目已有 `Kiro IDE token watch`、`distributed sync`、`auto reload` 逻辑耦合，直接整块照搬会引入回归。
  - `Service` 运行态 hook 与 management hook 并存时，若策略不统一，容易出现双重处理。
- 待确认:
  - 本轮按用户要求直接执行，不再额外中断确认；若遇到与现有未提交变更直接冲突，再回到确认。

## 模式说明

- 若执行模式为 `required-only`，本文件中的目标和范围仍需完整记录，但实施与跟踪只强制推进必备任务
