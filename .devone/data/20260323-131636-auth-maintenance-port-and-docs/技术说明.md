# 技术说明

## 文档元信息

- 任务名称: auth-maintenance-port-and-docs
- 任务目录: .devone/data/20260323-131636-auth-maintenance-port-and-docs
- 创建时间: 20260323-131636
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 实际改动摘要

- 本轮改动:
  - 新增 `auth-maintenance` 配置结构与 SDK 导出别名。
  - 在 `sdk/cliproxy/service.go` 接入运行态 `AuthMaintenance` 队列、hook、路径索引与删除/禁用动作。
  - 在 watcher 中新增 `SuppressAuthPath`、pending write debounce 与 stale generation 保护。
  - 新增配置测试、watcher 测试与 `sdk/cliproxy` 运行态维护测试。
  - 初始化 `.devone/wiki/`，补齐使用方式、配置方法与此前相关改动关系说明。
- 涉及文件/模块:
  - `internal/config/config.go`
  - `sdk/config/config.go`
  - `internal/watcher/watcher.go`
  - `internal/watcher/events.go`
  - `sdk/cliproxy/service.go`
  - `sdk/cliproxy/service_auth_maintenance.go`
  - `.devone/wiki/*`
- 未改动但已核对:
  - `internal/api/handlers/management/auth_files_runtime_hook.go`
  - `internal/api/handlers/management/auth_files_runtime_hook_test.go`
- 结构化回写提示:
  - 更新固定章节时，优先使用 `update-doc-section`
  - 更新 `## Wave 执行记录` 下的单个 `### Wave N` 时，优先使用 `append-wave-record`

## Wave 执行记录

### Wave 1

- 目标:
  - 在不回退既有语义的前提下移植 `AuthMaintenance` 机制。
- 改动:
  - 合并 watcher 自抑制与写入去抖。
  - 合并 `Service` 运行态维护队列。
  - 新增配置与测试。
  - 初始化 DevOne wiki。
- 结果:
  - `usage_limit_reached` 仍然只禁用。
  - generic `429` 不误伤。
  - `401` 达阈值后由后台队列删除。

## 验证记录

- 命令:
  - `go test ./internal/config ./internal/watcher ./sdk/cliproxy -run "Test"`
  - `go test ./internal/api/handlers/management -run "TestAuthRuntimeMaintenanceHook_"`
- 结果:
  - 通过
- 说明:
  - 全量 `go test ./internal/api/handlers/management ./sdk/cliproxy ...` 时，management 包存在一条与本轮无关的既有失败：`TestListAuthFiles_ExposesQuotaWindowFieldsAfterExecutionFailure`。本轮改动相关的 targeted tests 已通过。
  - 本轮收尾再次重跑相同 targeted tests，结果仍为通过。

## 风险、阻塞与未验证项

- 风险:
  - `auth-maintenance.delete-status-codes` 若手动加入 `429`，会扩大删除面，需要使用者自行承担配置风险。
- 阻塞:
  - 无
- 未验证项:
  - 未做整仓全量回归；仅完成本轮关键路径 targeted tests。

## 验收准备

- 可交给 `devone-acceptance` 的证据:
  - 需求、设计、测试设计已回写
  - 关键 targeted tests 通过
  - `.devone/wiki/` 已建立并同步本次知识
  - 组合配置示例已补齐，覆盖 `auth-runtime`、`auth-maintenance`、`distributed-sync`
- 仍需补充的证据:
  - 若要求更高置信度，可追加全量回归结果

## 验收结论

- 总结论:
  - accepted
- 需求映射:
  - [REQ-001] 通过；`usage_limit_reached` 仅产生 disable candidate，未进入 delete 路径。
  - [REQ-002] 通过；generic `429` 不会误禁用或误删除。
  - [REQ-003] 通过；已接入维护队列、路径去重、节流、watcher 自抑制与写入去抖。
  - [REQ-004] 通过；wiki 与任务包已补齐使用、配置和与既有 distributed sync 的关系说明。
- 知识层状态:
  - 一致
- 未验证项:
  - 未执行整仓全量回归；仅执行本轮关键路径 targeted tests。
