# 相关知识

## 文档元信息

- 任务名称: 吸收 CassiopeiaCode 优化能力并分阶段合并主线
- 任务目录: .devone/data/20260401-232630-吸收-CassiopeiaCode-优化能力并分阶段合并主线
- 创建时间: 20260401-232630
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 全量 (full)
- 当前阶段: execution
- 当前状态: in_progress

## 已知事实

- 现有实现:
  - 当前仓库 `RoutingConfig` 仅支持 `round-robin / fill-first`，见 `internal/config/config.go`、`sdk/cliproxy/builder.go`、`sdk/cliproxy/service.go`。
  - 当前仓库 `RoundRobinSelector` 已有 `ObserveResult`，但健康分未成为常态主选路因子，见 `sdk/cliproxy/auth/selector.go`。
  - 当前仓库 Codex 请求转换仍以 `gjson/sjson` 逐段修改 JSON，见 `internal/translator/codex/openai/chat-completions/codex_openai_request.go`。
  - 当前仓库不存在成型 `request-audit` hook。
  - 当前仓库 Codex executor 仍使用 `codex_cli_rs` 风格 header，见 `internal/runtime/executor/codex_executor.go` 与 `codex_websockets_executor.go`。
  - runtime auth maintenance 删除/禁用主入口在 `sdk/cliproxy/service_auth_maintenance.go` 与 `internal/api/handlers/management/auth_files_runtime_hook.go`。
  - 本轮修正前，service 层会把 `delete-status-codes: [429]` 和 `delete-quota-exceeded: true` 推进到删除候选；修正后两者统一转为 disable。
- 相关文档:
  - `.devone/wiki/index.md`
  - 本轮 CassiopeiaCode 对比分析结论
- 历史约束:
  - 不允许为了“先跑通”加入静默 fallback 或假成功路径。
  - 当前仓库在 Codex 链路已经积累了 Responses/compat 相关改动，阶段 2 和阶段 4 必须保留主线已有关键行为。

## 术语与定义

- `success-rate`：按 provider/model/auth 的时间衰减成功率做调度。
- `simhash`：对请求体做规范化后提取相似度哈希，用于相似请求稳定路由。
- `request blocker`：对已确认会触发 invalid request 的请求体进行短期阻断。
- `request-audit`：异步向外部 endpoint 发送请求/响应审计事件。
- `header 拟真`：将出站请求头调整为更接近目标原生客户端的形态。

## 调研结论

- 结论 1:
  - 最值得先吸收的是反馈式路由体系，而不是 header 细节。
- 结论 2:
  - 第二优先级是 Codex 请求转换性能优化；这一块必须“吸收结构化构造 + cache”，但不能丢掉当前仓库已有兼容补丁。
- 结论 3:
  - request-audit hook 价值明确，属于中优先级可观测性增强。
- 结论 4:
  - Codex header 拟真需要按点吸收，不能整包照搬。
- 结论 5:
  - `429` / quota exceeded 的账号维护语义必须作为主线保护约束固化，否则用户侧配置误差会把本应禁用的账号删掉。

## 交接差异矩阵

- 产品事实源:
  - 用户要求阶段化吸收并阶段合并、提交、推送。
- 知识事实源:
  - 本轮源码对比与结论。
- 实现事实源:
  - 当前仓库与目标仓库对应源码文件。
- 8 维摘要:
  - 当前仓库强项：已有主线兼容逻辑、AuthMaintenance 与 Responses 相关修复较新。
  - 目标仓库强项：反馈式路由、request cache、request-audit、header 拟真。
  - 最佳吸收策略：选择性移植，不做全量同步。

## 风险假设

- 假设:
  - 目标仓库中的 `success-rate / simhash / request blocker` 能在当前主线以较小冲突接入。
  - translator fast path 改造可以保留当前仓库已有关键兼容行为。
  - request-audit 与 header 拟真可以被拆成独立阶段，避免交叉回归。
- 验证方式:
  - 每阶段独立包测试
  - 每阶段文档与配置示例核对
  - 每阶段主线合并后再做阶段验收

## 知识升级候选

- 可升级为正式 wiki 的知识:
  - “反馈式账号路由”在当前仓库中的实现边界与配置方式
  - Codex 请求转换性能优化的主线实现原则
- 仍停留在任务级草稿的知识:
  - 目标仓库与当前仓库的逐文件差异细节
- 升级前缺口:
  - 需要至少完成阶段 1 或阶段 2 的真实落地与验证

## 待补证据

- 阶段 1 实施后的真实测试输出
- 阶段 2 translator 回归与兼容性证据
- 阶段 3 hook 接线与非阻塞证据
- 阶段 4 header 行为断言证据
