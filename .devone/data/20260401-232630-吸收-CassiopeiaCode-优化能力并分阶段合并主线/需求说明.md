# 需求说明

## 文档元信息

- 任务名称: 吸收 CassiopeiaCode 优化能力并分阶段合并主线
- 任务目录: .devone/data/20260401-232630-吸收-CassiopeiaCode-优化能力并分阶段合并主线
- 创建时间: 20260401-232630
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 全量 (full)
- 当前阶段: execution
- 当前状态: in_progress

## 问题定义

- 当前问题:
  - 当前仓库相较 `J:\_Dev\_me\_cpa\CLIProxyAPI-CassiopeiaCode-优化`，在账号路由策略、Codex 请求转换性能、异步 request-audit、以及 Codex 上游 header 拟真方面存在可确认缺口。
  - 当前仓库虽然已经有部分反馈式健康分逻辑，但未形成真正可配置的 `success-rate / simhash` 选路体系，也没有 invalid request blocker 与 request body simhash 链路。
  - 当前仓库的 Codex Chat Completions -> Responses 请求转换仍以 `gjson/sjson` 逐段改写为主，复杂请求下重复整段 JSON 重算开销较高。
  - 执行阶段新增发现：运行态 auth 维护仍存在把 `429` / quota exceeded 账号推进到自动删除链路的风险，需要收敛为自动禁用。
- 影响对象:
  - `sdk/cliproxy/auth/*` 路由与执行调度链路
  - `internal/translator/codex/openai/chat-completions/*` 请求转换链路
  - `internal/runtime/executor/codex*.go` Codex HTTP/WS 出站请求行为
  - 配置示例、使用文档、阶段发布与合并流程
- 触发背景:
  - 用户已明确要求使用 `$devone` 建立任务包，按推荐顺序逐阶段吸收目标仓库的优秀特性。
  - 用户要求每个阶段都必须：实现、验证、编写使用说明、提供配置示例、合并主工作区、提交并推送。
- 为什么现在处理:
  - 这些能力会直接影响复杂账号池下的稳定性、Codex 请求链路性能与排障能力。
  - 逐阶段吸收并在每阶段合并主线，可以把回归面控制在单一主题内，降低一次性大合并风险。

## 目标与成功标准

- 目标 1:
  - 以阶段化方式吸收目标仓库中已确认优于当前版本的能力，不做整仓照搬。
- 目标 2:
  - 每个阶段都形成独立可验证交付：代码、测试/验证证据、使用说明、配置示例、合并记录、提交记录、推送记录。
- 目标 3:
  - 保留当前仓库已经存在且更贴近当前主线的兼容逻辑，不因直接搬运目标仓库代码而破坏现有行为。
- 成功标准:
  - 每一阶段都能单独通过验收并合并到 `main`。
  - 每一阶段都产出用户可直接使用的文档与示例。
  - 每一阶段合并后都完成 git commit，并推送到 `gdtiti`。

## 范围边界

### 本次必须完成

- 阶段 1：吸收反馈式路由体系
  - `success-rate` selector
  - `simhash` selector
  - request body simhash 分析
  - invalid request blocker
  - 路由配置、使用说明、配置示例
- 阶段 2：吸收 Codex 请求转换性能优化
  - 结构化构造替代高频 `sjson.Set*`
  - parsed request cache
  - 保留当前仓库已存在的重要兼容逻辑
  - 性能优化说明与配置/使用示例
- 阶段 3：吸收异步 request-audit hook
  - 配置结构
  - hook 实现与接线
  - 使用说明与配置示例
- 阶段 4：吸收可控的 Codex header 拟真增强
  - 只吸收可验证且与当前主线不冲突的部分
  - 增补相关测试、使用说明与示例
- 每阶段结束后：
  - 在独立 worktree/分支完成实现
  - 跑对应验证
  - 合并回主工作区 `main`
  - 生成提交并推送到 `gdtiti`

### 本次明确不做

- 不直接把目标仓库整批文件覆盖到当前仓库
- 不无差别同步目标仓库所有 README、文档、历史 commit 风格
- 不引入未经验证的 silent fallback、假数据、演示逻辑
- 不处理与上述四个主题无关的额外重构
- 不动主工作区当前未跟踪的 `.magi/`、`.tmp-go/`、`.windsurfrules`

## 验收真相陈述

- [REQ-001] 阶段 1 必须把 `success-rate / simhash / request blocker` 以当前仓库风格接入主链路，并补齐配置示例与使用说明。
- [REQ-002] 阶段 2 必须完成 Codex 请求转换性能优化，并证明不破坏当前仓库已有关键兼容逻辑。
- [REQ-003] 阶段 3 必须完成 request-audit hook 的配置、接线、文档与示例。
- [REQ-004] 阶段 4 必须只吸收经验证安全的 Codex header 拟真增强，并补齐对应测试与文档。
- [REQ-005] 每个阶段都必须独立完成：fresh evidence、阶段合并、commit、推送 `gdtiti`、使用说明、配置示例。
- [REQ-006] `429` 与 quota exceeded 账号必须自动禁用，不得再因 runtime auth maintenance 逻辑被删除；原有 `401` 阈值删除、`codex-max-request-count` 删除、quota probe `401` 删除保持不变。

## 约束与依赖

- 技术约束:
  - 必须以当前仓库为主真相源，目标仓库只作为可吸收实现来源。
  - 每阶段必须小步实现并保持可回滚，不做一次性大合并。
  - Windows PowerShell 文本读写必须显式使用 UTF-8。
- 业务约束:
  - 用户要求按推荐顺序推进，不得打乱阶段顺序。
  - 每阶段都要有文档与示例，不能只交代码。
- 外部依赖:
  - `gdtiti` 远端可推送。
  - Go 测试环境可运行对应包测试。
  - DevOne packet/worktree 脚手架可正常使用。

## 风险与待确认项

- 风险:
  - 当前主工作区存在未跟踪文件，若直接在主工作区开发或合并，容易污染阶段提交边界。
  - 阶段 2 与阶段 4 都会触碰 Codex 关键链路，若直接照搬目标仓库代码，可能覆盖当前仓库新增的兼容修复。
  - 阶段 4 的 header 拟真增强收益真实存在，但也最容易影响现有连续性、会话与诊断逻辑。
- 待确认:
  - 默认每阶段推送目标远端为 `gdtiti`；若后续需要同时推送 `origin`，作为额外动作单独记录。
  - 阶段 2 对“无新增配置”的阶段，仍需提供“无需新增配置/沿用现有配置”的示例片段，以满足用户的“每阶段配置示例”要求。

## 模式说明

- 若执行模式为 `required-only`，本文件中的目标和范围仍需完整记录，但实施与跟踪只强制推进必备任务
