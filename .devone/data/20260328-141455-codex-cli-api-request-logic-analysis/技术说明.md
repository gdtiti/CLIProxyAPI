# 技术说明

## 文档元信息

- 任务名称: codex-cli-api-request-logic-analysis
- 任务目录: .devone/data/20260328-141455-codex-cli-api-request-logic-analysis
- 创建时间: 20260328-141455
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: execution
- 当前状态: draft

## 实际改动摘要

- 本轮改动: 未修改业务代码；完成请求链路分析并把结论回写到任务文档包。
- 本轮补充: 已把“Codex CLI 看起来从流式切到非流式”的静态分析结论回写到任务包，并同步一份项目级 wiki 知识页。
- 涉及文件/模块:
  - `sdk/api/handlers/openai/openai_handlers.go`
  - `sdk/api/handlers/openai/openai_responses_handlers.go`
  - `sdk/api/handlers/openai/openai_responses_websocket.go`
  - `sdk/api/handlers/openai/endpoint_compat.go`
  - `internal/translator/codex/openai/chat-completions/codex_openai_request.go`
  - `internal/runtime/executor/codex_executor.go`
  - `internal/runtime/executor/codex_websockets_executor.go`
  - `internal/runtime/executor/payload_helpers.go`
  - `sdk/cliproxy/auth/selector.go`
  - `sdk/cliproxy/auth/oauth_model_alias.go`
  - `internal/watcher/synthesizer/config.go`
  - `internal/registry/model_updater.go`
  - `internal/registry/model_definitions.go`
  - `internal/registry/models/models.json`
  - `internal/config/config.go`
  - `config.example.yaml`
  - `config.yaml`
- 未改动但已核对:
  - 当前 `config.yaml` 中与 Codex 直接相关的扩展配置大多仍处于注释状态。
- 结构化回写提示:
  - 更新固定章节时，优先使用 `update-doc-section`
  - 更新 `## Wave 执行记录` 下的单个 `### Wave N` 时，优先使用 `append-wave-record`
  - 更新 `## 并行批次记录` 下的单个 `### Batch N` 时，优先使用 `append-parallel-batch`

## Wave 执行记录

### Wave 1

- 目标: 锁定 Codex CLI 转 API 的请求逻辑特征与扩展点。
- 改动: 只读检查关键 handler / translator / executor / selector / registry / config 文件，并回写调研结论。
- 结果: 已形成“当前特征”“配置优先调整点”“代码优先调整点”“保持最新版本的关键位置”四类结论。

### Wave 2

- 目标: 判断 Codex CLI 渠道下“流式用着用着突然切到非流式”是否来自代理侧中途改道。
- 改动: 只读核对 `/v1/responses`、`/v1/responses/compact`、Responses WebSocket、Codex HTTP/WS executor 与 translator；同步任务包知识与 wiki。
- 结果: 已确认当前实现不存在“同一条已发出 payload 的流式请求中途切非流式”的逻辑；更可能是后续请求形态混用或 WebSocket prewarm 短路。

## 并行批次记录

- 使用提示:
  - 当前 wave 若使用子代理并行，优先把边界、写集、验证目标与汇总结论落到这里
  - 单个批次卡片优先通过 `append-parallel-batch` 回写

### Batch 1

- batch_type:
- task_ids:
- write_set:
- verify_target:
- forbidden_actions:
- single_writer:
- findings:
- results:

## 验证记录

- 命令:
  - `Get-Content -Encoding utf8 ...`
  - `rg -n ...`
- 结果: 已交叉确认入口分流、转换器、执行器、鉴权与模型目录链路。
- 补充结果: 已确认 `ExecuteStreamWithAuthManager` 首字节后不会 retry 改道；已确认 Codex 非流式 `Execute` 是“上游流式、下游聚合”；已确认 Responses WebSocket 的 `generate=false` prewarm 会本地合成 `created/completed`。
- 说明: 本轮未执行运行时联调或自动化测试。

## 风险、阻塞与未验证项

- 风险: 固定版本常量、Beta header 与模型目录刷新机制位于不同模块，后续若只改一处容易产生“部分最新、部分陈旧”的不一致。
- 阻塞: 无。
- 未验证项: 未对真实上游进行 HTTP/WS 行为验证；未验证当前默认头是否仍完全匹配最新官方 Codex CLI。

## 验收准备

- 可交给 `devone-acceptance` 的证据: 目前只有只读分析证据，不足以进入代码交付验收。
- 仍需补充的证据: 若进入实现，需要补代码变更、测试输出和实际请求行为验证。
