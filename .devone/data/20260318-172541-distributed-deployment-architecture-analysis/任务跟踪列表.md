# 任务跟踪列表

## 文档元信息

- 任务名称: distributed-deployment-architecture-analysis
- 任务目录: .devone/data/20260318-172541-distributed-deployment-architecture-analysis
- 创建时间: 20260318-172541
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 执行策略

- 当前选择:
  - `full`: 全量执行并跟踪必备任务与可选任务
  - `required-only`: 只执行必备任务；可选任务保留记录但默认不推进
- 本次实际模式: required-only

## 状态枚举

- `pending`: 尚未开始
- `in_progress`: 正在执行
- `blocked`: 有明确阻塞
- `done`: 已完成且已有证据
- `deferred`: 已确认延期
- `skipped_optional`: 因执行模式或用户选择而跳过的可选任务
- `rejected`: 经验收失败

## 任务包状态枚举

- `in_progress`: 当前任务包仍在推进
- `blocked`: 当前任务包存在明确阻塞
- `done`: 实现已完成，待正式验收
- `accepted`: 验收通过
- `conditionally_accepted`: 有条件通过
- `rejected`: 验收未通过

## 必备任务

### R1. 锁定问题定义、Phase 1 范围与执行模式

- 类型: 必备任务
- 状态: done
- 前置条件: 已确认项目根目录和 task packet
- 产出:
  - `需求说明.md`
  - `任务跟踪列表.md`
- 验证: Phase 1 目标、非目标与 `required-only` 模式已同步
- 备注:
  已明确当前波次只交付 config/auth 分布式同步基础设施，不把统计缓存混入本轮。

### R2. 形成可批准设计与测试方案

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R1 已完成
  - 已完成最小必要调研
- 产出:
  - `设计书.md`
  - `单元测试设计文档.md`
- 验证: 锁定设计、风险、阶段计划和测试设计已与实现边界对齐
- 备注:
  已锁定 `PG version/outbox + Redis Pub/Sub + PG poll reconcile` 方案，放弃
  Redis-only 版本日志和细粒度 patch 首版实现。

### R3. 完成 Phase 1 最小可验证实现

- 类型: 必备任务
- 状态: done
- 前置条件:
  - 设计已批准
  - 关键依赖已确认
- 产出:
  - 代码改动
  - `技术说明.md`
- 验证:
  `go test ./internal/store ./internal/distributedsync ./sdk/cliproxy -count=1 -timeout 60s`
  `go build ./cmd/server`
- 备注:
  已落地 distributed sync 配置、PostgreSQL version/outbox、Redis notifier、
  manager、服务接线及第一批单元测试。

### R4. 完成验收门禁核对

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R3 已完成
  - fresh evidence 已收集
- 产出:
  - 需求映射
  - 门禁结论
- 验证:
  - REQ-001~REQ-004 门禁已核对
  - go test ./internal/store ./internal/distributedsync ./sdk/cliproxy -count=1 -timeout 60s 通过
  - go build ./cmd/server 通过
- 备注: accepted 仅针对 Phase 1 / required-only 范围；真实多节点联调与统计窗口能力仍在范围外

## 可选任务

### O1. 统计窗口缓存实现

- 类型: 可选任务
- 状态: skipped_optional
- 前置条件:
  - Phase 1 基础设施稳定
  - 用户允许扩展范围
- 产出:
  - 3h / 6h / 24h / 日 / 月统计缓存
  - 相关查询与失效逻辑
- 验证: 统计窗口测试与真实查询证据
- 备注:
  因本次为 `required-only`，本轮只保留设计，不推进实现。

### O2. 真实多节点联调与更深回归

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - Phase 1 代码闭环通过
  - 可用的 PostgreSQL / Redis / 多节点环境已准备
- 产出:
  - 多节点联调结果
  - 更深回归与对账证据
- 验证: 真实环境证据已记录到 `技术说明.md`
- 备注:
  当前尚未在真实多节点环境执行。

## 当前波次

- 当前波次: Wave 1: config/auth 分布式同步 Phase 1 最小闭环
- 当前聚焦任务: R4 验收门禁核对
- 不在本波次的任务:
  统计窗口缓存实现
  真实多节点联调

## 变更记录

- [20260318-174448] 初始化 discovery 文档，锁定问题定义、设计草案和测试草案
- [20260318-201741] 进入 execution，回写需求/设计/技术/测试文档并标记
  R1-R3 完成，R4 进入验收门禁核对
- [20260318-213552] 补充双节点联调手册的同机双容器 docker-compose 模板、共享
  配置样例，并校正文档中的 Docker 管理接口前提
- [20260318-231320] 新增联调检查清单与验证命令速查表，并接入主手册与 docs 索引
- [20260318-231927] 新增联调异常场景判定表，并接入主手册、速查表与 docs 索引
- [20260322-135610] 任务块=R4；状态=done；验证=3；备注=已更新
- [20260322-135610] 补 fresh evidence 并完成 Phase 1 required-only 验收
- [20260322-154217] 用户明确要求：对已实施或正在实施的任务，不做删除式清理，优先完成并提交整合回主工作区
- [20260322-154500] 主工作区已补齐 distributed sync 依赖、测试与联调文档，并修复 `internal/store/postgresstore.go` 半整合问题
- [20260322-154501] fresh evidence 已在主工作区取得：`go test ./internal/store ./internal/distributedsync ./sdk/cliproxy -count=1 -timeout 60s`、`go build ./cmd/server`
