- 真相源:
  业务对象的权威持久化位置，本方案指 PostgreSQL。
- spool:
  节点本地镜像目录，供现有 file-based watcher 和 runtime 加载逻辑使用。
- outbox:
  与业务数据同事务提交的事件表，用来保证“数据提交”与“事件发布”一致。
- 增量同步:
  节点按对象版本只拉取变更部分，而不是每次全量 reload。
- 全量回灌:
  当节点检测到事件缺口、版本异常或 apply 失败时，从 PG 真相源重新镜像到
  本地。
