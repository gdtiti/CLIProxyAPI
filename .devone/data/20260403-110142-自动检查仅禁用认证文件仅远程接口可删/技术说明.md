# 技术说明

## 文档元信息

- 任务名称: 自动检查仅禁用认证文件仅远程接口可删
- 任务目录: .devone/data/20260403-110142-自动检查仅禁用认证文件仅远程接口可删
- 创建时间: 20260403-110142
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: execution
- 当前状态: draft

## 实际改动摘要

- 本轮改动:
  - 将 `sdk/cliproxy/service_auth_maintenance.go` 中自动 `401` 阈值、兼容 delete 配置字段、旧 pending delete 状态统一收口到 disable 语义，不再让自动维护进入 delete candidate。
  - 将 `internal/api/handlers/management/auth_files_runtime_hook.go` 中 `codex-max-request-count`、quota probe `401`、`401` 阈值等 management 自动维护链路统一改为 `handleManagedAuthDisable(...)`。
  - 将 `internal/api/handlers/management/auth_files_runtime_hook_test.go` 与 `sdk/cliproxy/service_auth_maintenance_test.go` 的删除断言改为“文件保留 + disabled=true + 运行态 Disabled + disable status message”。
  - 同步更新 `internal/config/config.go`、`config.example.yaml`、`.devone/wiki/projects/runtime-auth-maintenance.md` 的字段注释与使用说明，明确自动检查只能禁用，不能删除。
- 涉及文件/模块:
  - `sdk/cliproxy/service_auth_maintenance.go`
  - `internal/api/handlers/management/auth_files_runtime_hook.go`
  - `internal/api/handlers/management/auth_files_runtime_hook_test.go`
  - `sdk/cliproxy/service_auth_maintenance_test.go`
  - `internal/config/config.go`
  - `config.example.yaml`
  - `.devone/wiki/projects/runtime-auth-maintenance.md`
- 未改动但已核对:
  - `internal/api/server.go`
  - `internal/api/handlers/management/auth_files.go`
  - direct delete API 仍保留：
    - `internal/api/server.go:694` -> `DELETE /auth-files`
    - `internal/api/server.go:696-698` -> recycle bin list / restore / purge
    - `internal/api/handlers/management/auth_files.go:919` -> `DeleteAuthFile`
    - `internal/api/handlers/management/auth_files.go:1113` -> `deleteAuthFileByName`
- 结构化回写提示:
  - 更新固定章节时，优先使用 `update-doc-section`
  - 更新 `## Wave 执行记录` 下的单个 `### Wave N` 时，优先使用 `append-wave-record`
  - 更新 `## 并行批次记录` 下的单个 `### Batch N` 时，优先使用 `append-parallel-batch`

## Wave 执行记录

### Wave 1

- 目标:
  - 把所有自动检查/自动判断链路从“自动删除 auth 文件”改成“只自动禁用 auth 文件”，同时保留远程直接删除接口。
- 改动:
  - 已把自动维护主链改为 disable。
  - 已把关键测试从 delete 断言切到 disable 断言。
  - 已把配置注释与 wiki 文档从 delete 语义改成 disable 语义。
- 结果:
  - 自动维护语义已统一到 disable。
  - direct delete API 未改动。
  - Go 定向测试未能执行，需在具备 Go 环境后补 fresh evidence。

## 并行批次记录

- 使用提示:
  - 当前 wave 若使用子代理并行，优先把边界、写集、验证目标与汇总结论落到这里
  - 单个批次卡片优先通过 `append-parallel-batch` 回写

### Batch 1

- batch_type:
- task_ids:
- write_set:
- verify_target:
- forbidden_actions:
- single_writer:
- findings:
- results:

## 验证记录

- 命令:
  - `rg -n 'auth-files' internal/api/server.go`
  - `rg -n 'DELETE \"/auth-files\"|DeleteAuthFile\\(|deleteAuthFileByName\\(|recycle-bin' internal/api/server.go internal/api/handlers/management/auth_files.go`
  - `git diff --check -- internal/api/handlers/management/auth_files_runtime_hook_test.go sdk/cliproxy/service_auth_maintenance_test.go internal/config/config.go config.example.yaml .devone/wiki/projects/runtime-auth-maintenance.md`
  - `go test ./sdk/cliproxy ./internal/api/handlers/management ./internal/config -count=1`
- 结果:
  - direct delete API 静态核对通过，删除入口仍只在 management delete API / recycle bin 管理接口。
  - diff 格式检查通过（仅有行尾 warning）。
  - `go test` 未执行成功：PowerShell 报错 `The term 'go' is not recognized...`
- 说明:
  - 当前环境缺少 Go 工具链，因此只能先提交静态证据与测试改造结果，不能宣称运行态验证已通过。

## 风险、阻塞与未验证项

- 风险:
  - 由于未跑 Go 测试，暂不能排除编译期或断言级回归。
- 阻塞:
  - 当前环境缺少 `go` / `gofmt`，无法执行目标测试命令。
  - DevOne `worktree-create` 本轮误判仓库非 git work tree，且主工作区已有脏改，导致 R2.5 暂无法安全推进。
- 未验证项:
  - `internal/api/handlers/management/auth_files_runtime_hook_test.go`
  - `sdk/cliproxy/service_auth_maintenance_test.go`
  - `internal/config/auth_maintenance_config_test.go`

## 验收准备

- 可交给 `devone-acceptance` 的证据:
  - 自动维护主链代码已改为 disable
  - direct delete API 静态核对结果
  - 测试/配置/wiki 语义同步结果
- 仍需补充的证据:
  - 具备 Go 环境后的 `go test ./sdk/cliproxy ./internal/api/handlers/management ./internal/config -count=1`
