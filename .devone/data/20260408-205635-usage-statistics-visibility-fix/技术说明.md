# 技术说明

## 文档元信息

- 任务名称: usage-statistics-visibility-fix
- 任务目录: .devone/data/20260408-205635-usage-statistics-visibility-fix
- 创建时间: 20260408-205635
- 工作区策略: 独立 Worktree (worktree)
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 实际改动摘要

- 本轮改动:
  - 后端 PG 快照查询新增明细回填：`QuerySnapshot` 现在同时读取 `usage_aggregates` 与 `usage_details`，返回包含 `models.details` 的统一结构。
  - 后端新增 `QueryDetailsByRange(from,to,instance,limit)`，支持实例过滤与可选 limit。
  - 管理接口 `parseTimeRange` 支持 `range=all`，起始时间固定为 `2000-01-01T00:00:00Z`。
  - 管理中心前端在无 details 场景增加聚合回退：
    - `filterUsageByTimeRange` 不再把无 details 的模型直接过滤掉。
    - 小时/日趋势在 details 缺失时回退使用 `requests_by_hour|day` / `tokens_by_hour|day`。
    - `MonitorPage` 本地过滤改为仅按 API key 过滤，不再二次按 details 截断。
- 涉及文件/模块:
  - `internal/usage/query.go`
  - `internal/api/handlers/management/usage.go`
  - `internal/usage/query_test.go`
  - `internal/api/handlers/management/usage_test.go`
  - `../Cli-Proxy-API-Management-Center/src/utils/usage.ts`
  - `../Cli-Proxy-API-Management-Center/src/pages/MonitorPage.tsx`
- 未改动但已核对:
  - `internal/usage/logger_plugin.go`（快照结构字段定义）
  - `internal/usage/pg_schema.go`（`usage_details.instance_id` 与索引）

## Wave 执行记录

### Wave 1

- 目标: 修复 PG 路径统计契约缺口，避免前端页面空白。
- 改动:
  - 完成后端查询与时间范围修复。
  - 完成前端聚合回退与监控页过滤逻辑收敛。
  - 增补对应单测文件。
- 结果: 关键路径完成，后端 usage 包测试通过，前端 type-check 通过。

### Closeout

- 目标: 完成收尾对账，确认当前任务是否具备 merge readiness。

- 改动:
  - 删除已确认的 worktree 临时文件与前端旧任务包目录。
  - 分别提交后端 worktree 提交 60c4fa1 与前端仓库提交 720042d。
  - 复核后端 worktree 与前端仓库 git status --short 均为空。
  - 执行 worktree-merge --into main --confirm --json。

- 结果:
  - worktree-merge --into main --json dry-run 已通过，但真实执行在 git merge --no-edit origin/main 步骤失败。
  - 当前 worktree 已进入 merge conflict 状态，HEAD 仍为 60c4fa1f1874a4fbefdf52c39fcf2e76ecec430。
  - 主要冲突文件包括：config.example.yaml、internal/api/handlers/management/auth_files.go、internal/api/modules/amp/response_rewriter.go、internal/runtime/executor/codex_executor.go、internal/runtime/executor/codex_websockets_executor.go、internal/runtime/executor/helps/usage_helpers.go、internal/runtime/executor/iflow_executor.go、sdk/cliproxy/auth/conductor.go、sdk/cliproxy/service.go 等共 13 个冲突项。
  - 当前 closeout 结论回退为 blocked；worktree 保持保留，等待用户决定是继续解冲突还是中止本次 merge。

## 并行批次记录

- 本波次未使用子代理并行。

## 验证记录

- 命令:
  - `HTTP_PROXY=http://127.0.0.1:9892 HTTPS_PROXY=http://127.0.0.1:9892 ALL_PROXY=http://127.0.0.1:9892 go test ./internal/api/handlers/management -run ParseTimeRange_All -count=1`
  - `HTTP_PROXY=http://127.0.0.1:9892 HTTPS_PROXY=http://127.0.0.1:9892 ALL_PROXY=http://127.0.0.1:9892 go test ./internal/usage -count=1`
  - `pnpm type-check`（`Cli-Proxy-API-Management-Center`）
  - `Invoke-WebRequest -Uri 'http://127.0.0.1:34985/v0/management/usage?range=all' -Headers @{ Authorization = 'Bearer devone-accept-20260408' }`
  - `jshook network_intercept_list / dom_find_by_text / page_evaluate / page_screenshot`（聚合-only 回包验收）
  - `jshook network_intercept_disable(ruleId='608c6fe8') / page_reload / page_type / page_click / page_evaluate / page_screenshot`（真实后端页面验收）
- 结果:
  - `go test ./internal/api/handlers/management -run ParseTimeRange_All -count=1` 通过。
  - `go test ./internal/usage -count=1` 通过。
  - `pnpm type-check` 通过。
  - 运行态接口抽样返回 `200`；`usage.total_requests=2`、`usage.total_tokens=250`、`usage.apis.test-client-key.models.gpt-4o-mini.details` 数量为 `2`。
  - 聚合-only 验收中，拦截规则 `608c6fe8` 将 `/v0/management/usage` 回包固定为 `details: []` 且 `total_requests=12`、`total_tokens=4321`；页面仍显示 KPI `12`、`4.32K` 和模型 `gpt-4o-mini`。
  - 解除拦截并重新登录后，真实后端页面显示请求数 `2`、Token `250`、模型 `gpt-4o-mini`、请求日志 `2` 条。
- 说明:
  - 聚合-only 场景与真实后端场景都已完成页面级可观察验证，截图分别保存到：
    - `acceptance/monitor-fallback-after-fix-validated-20260408-continued.png`
    - `acceptance/monitor-real-backend-after-login-20260408-continued.png`
  - 聚合-only 验收使用前端响应拦截复现服务器合法契约变体；真实后端抽样则证明当前服务实例已能返回带 `details` 的离线持久化统计数据。
  - 验证完成后已移除拦截规则 `608c6fe8`，避免影响后续真实数据观察。

## 风险、阻塞与未验证项

- 风险:
  - `defaultSnapshotDetailsLimit=50000` 的明细上限仍然存在；超大时间窗口下可能出现“聚合值大于返回明细数”的可预期差异，需要后续分页或提示策略。
  - 当后端只返回聚合字段、完全不返回 request/detail 级记录时，`ChannelStats` / `FailureAnalysis` / `RequestLogs` 这类依赖明细的分区仍会显示“暂无数据”；这符合当前数据契约，不属于本次显示修复回退失败。
- 阻塞:
  - 当前无阻塞项。
- 未验证项:
  - PT-001 独立属性测试框架未补建；本轮已用表驱动单测 + 运行态接口抽样 + 页面验收覆盖锁定需求。

## 验收准备

- 可交给 `devone-acceptance` 的证据:
  - 后端 `internal/usage` 包测试通过。
  - 后端 `internal/api/handlers/management` 的 `ParseTimeRange_All` 目标测试通过。
  - 前端管理中心 `pnpm type-check` 通过。
  - 运行态接口抽样证明 `/v0/management/usage?range=all` 返回 `200`，且统计数据已包含持久化明细。
  - 聚合-only 回包场景页面验收通过，截图：`acceptance/monitor-fallback-after-fix-validated-20260408-continued.png`。
  - 真实后端回包场景页面验收通过，截图：`acceptance/monitor-real-backend-after-login-20260408-continued.png`。
- 仍需补充的证据:
  - 无必备证据缺口。
- 本轮验收结论:
  - accepted（锁定需求、运行态接口、聚合-only 页面回退、真实后端页面展示均已补齐 fresh evidence）。
