# 相关知识

## 文档元信息

- 任务名称: usage-statistics-visibility-fix
- 任务目录: .devone/data/20260408-205635-usage-statistics-visibility-fix
- 创建时间: 20260408-205635
- 工作区策略: 独立 Worktree (worktree)
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 已知事实

- 现有实现:
  - `GetUsageStatistics` 在 PostgreSQL 场景下会通过 `QuerySnapshot` 返回聚合统计，并补回 `models.details` 明细。
  - `QuerySnapshot` 现在同时读取 `usage_aggregates` 与 `usage_details`；运行态抽样显示 `/v0/management/usage?range=all` 已返回 `details` 数量 `2`。
  - `parseTimeRange` 已支持 `range=all`，不再回落到默认 24h。
  - 管理中心前端的 KPI、模型分布、日/小时趋势图已经支持“仅聚合字段、无 details”场景的回退计算。
- 相关文档: 本任务包、`.devone/wiki/projects/devone-worktree-ledger.md`、管理中心仓库监控页相关实现。
- 历史约束: 不接受静默降级、伪成功或用假数据冒充真实统计展示。

## 术语与定义

- details: 单次请求/时间点级别明细（用于细粒度过滤与趋势重建）。
- aggregates: 按天/模型聚合的统计计数与 token 汇总。

## 调研结论

- 结论 1: 统计页恢复显示，依赖后端补齐 PG 明细契约和前端去除对 details 的单一路径依赖，两侧缺一不可。
- 结论 2: 页面主区域在聚合-only 回包与真实后端回包两种场景下都已通过运行态可观察验证。
- 结论 3: merge readiness 已通过，但真实 worktree-merge --confirm 在合并 origin/main 时发生 13 个冲突项，当前阻塞重新回到 merge conflict 处理。
- 结论 4: 前端管理中心仓库本地提交已完成，但它并不参与这次后端仓库的 worktree-merge --confirm；前端远端同步仍需单独决策。

## 交接差异矩阵

- 产品事实源: 页面不可见统计数据。
- 知识事实源: docs 与配置约束。
- 实现事实源: 代码路径与函数行为。
- 8 维摘要: 需求/实现不一致，数据契约缺失，时间窗口默认过窄，配置默认关闭采集。

## 风险假设

- 假设: closeout 目标分支使用 `origin/main`。
- 验证方式: `git ls-remote --heads origin` 返回 `refs/heads/main`；`worktree-merge --into main` dry-run 被当前 worktree 脏改动拦截。
- 当前风险:
  - `defaultSnapshotDetailsLimit=50000` 上限仍在；超大窗口下可能出现聚合值大于明细返回数。
  - 纯聚合回包下，依赖 request/detail 级数据的 `ChannelStats` / `FailureAnalysis` / `RequestLogs` 仍可能显示“暂无数据”，这属于当前数据契约边界。

## 知识升级候选

- 可升级为正式 wiki 的知识:
  - PostgreSQL usage 快照必须与内存统计路径保持字段契约一致，不能只返回 aggregates 不返回前端依赖的结构。
  - 监控页图表与 KPI 不能把 `details` 当作唯一事实源，必须支持聚合字段回退。
- 仍停留在任务级草稿的知识:
  - 本轮具体文件改动与截图证据。
- 升级前缺口:
  - 还未完成跨仓库提交与远端 closeout；当前只完成 ledger 级登记。

## 待补证据

- 功能性证据已补齐。
- 当前待处理项不是统计修复，而是当前 worktree 的 merge conflict。
- 需要用户决定：继续在当前 worktree 解冲突，还是先执行 git merge --abort 回到 60c4fa1。
