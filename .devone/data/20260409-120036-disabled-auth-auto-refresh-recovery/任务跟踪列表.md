# 任务跟踪列表

## 文档元信息

- 任务名称: disabled-auth-auto-refresh-recovery
- 任务目录: .devone/data/20260409-120036-disabled-auth-auto-refresh-recovery
- 创建时间: 20260409-120036
- 工作区策略: 独立 Worktree (worktree)
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- Worktree 目录: .devone/worktree/20260409-120036-disabled-auth-auto-refresh-recovery
- Worktree 分支: devone/20260409-120036-disabled-auth-auto-refresh-recovery
- 开发端口: 35983
- 当前阶段: completion
- 当前状态: in_progress

## 执行策略

- 当前选择:
  - `full`: 全量执行并跟踪必备任务与可选任务
  - `required-only`: 只执行必备任务；可选任务保留记录但默认不推进
- 本次实际模式: required-only
- 工作流日志: `.devone/wiki/logs/devone-workflow-log.md`（只用于人工检查，不作为平时知识读取入口）
- 结构化回写提示:
  - 更新阶段、波次、聚焦任务时，优先使用 `update-status`
  - 更新单个任务块的类型、状态、前置条件、产出、验证或备注时，优先使用 `update-task-block`
  - 模式切换时，优先使用 `sync-mode`

## 状态枚举

- `pending`: 尚未开始
- `in_progress`: 正在执行
- `blocked`: 有明确阻塞
- `done`: 已完成且已有证据
- `deferred`: 已确认延期
- `skipped_optional`: 因执行模式或用户选择而跳过的可选任务
- `rejected`: 经验收失败

## 任务包状态枚举

- `in_progress`: 当前任务包仍在推进
- `blocked`: 当前任务包存在明确阻塞
- `done`: 实现已完成，待正式验收
- `accepted`: 验收通过
- `conditionally_accepted`: 有条件通过
- `rejected`: 验收未通过

## 必备任务

### R1. 锁定问题定义与执行模式

- 类型: 必备任务
- 状态: done
- 前置条件: 已确认项目根目录
- 产出:
  - `需求说明.md` 的问题定义、目标、范围
  - 记录执行模式
- 验证:
  - `需求说明.md` 已补齐问题、目标、范围、验收真相
  - `任务跟踪列表.md` 与模式记录一致
- 备注:
  - 已锁定 `devone / required-only / classic / worktree`

### R2. 形成可批准设计与阶段计划

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R1 已完成
  - 已收集最小必要上下文
- 产出:
  - `设计书.md`
  - `相关知识.md`
  - `单元测试设计文档.md` 初稿
- 验证:
  - 已给出 3 条实现路径与推荐方案
  - 已锁定入口模块、关键调用链、风险与测试计划
- 备注:
  - 设计默认只恢复有系统恢复标记的 disabled `codex` auth

### R2.5. 创建 worktree 分支与开发端口

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R2 已完成
  - 设计已批准
  - 已确认执行基线不是当前本地主工作区 `main`
- 产出:
  - `.devone/worktree/<任务id>/` 目录可用
  - 独立 git 分支已创建/已绑定到 worktree
  - 端口已分配并写回文档元信息
- 验证:
  - `worktree-status` 显示 worktree 已创建且状态清晰
  - `Worktree 目录 / Worktree 分支 / 开发端口` 已写入 `## 文档元信息`
- 备注: worktree=.devone/worktree/20260409-120036-disabled-auth-auto-refresh-recovery；branch=devone/20260409-120036-disabled-auth-auto-refresh-recovery；port=35983

### R3. 完成最小可验证实现

- 类型: 必备任务
- 状态: done
- 前置条件:
  - 设计已批准
  - 新 worktree 已创建
  - 关键依赖已确认
- 产出:
  - disabled recovery 代码改动
  - recovery 日志能力
  - `技术说明.md`
- 验证:
  - 至少一条“仍限额保持 disabled”验证通过
  - 至少一条“额度恢复自动 unlock”验证通过
- 备注:
  - 本轮不含前端页面改造
  - 已完成 recovery 定向测试；全包仍有与本轮无关的既有失败，见 `技术说明.md`

### R4. 完成验收门禁核对

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R3 已完成
  - 本轮 fresh evidence 已收集
- 产出:
  - 需求映射
  - 门禁结论
  - completion gate 准备记录
- 验证:
  - acceptance audit `ready=true`
  - `go test ./sdk/cliproxy -run 'TestAuthMaintenanceRecoveryCandidateForAuth_RequiresSystemMarker|TestRecoverAuthMaintenanceCandidate_RefreshesAndUnlocksAuth|TestRecoverAuthMaintenanceCandidate_KeepsDisabledWhenQuotaNotRecovered|TestRecoverAuthMaintenanceCandidate_ClearsMarkerOnRefreshTokenReused' -v` 通过
  - `go test ./internal/api/handlers/management -run 'TestAuthRuntimeMaintenanceHook_DisablesAuthFileAfterUnauthorizedThreshold|TestAuthRuntimeMaintenanceHook_DisablesCodexAuthFileAfterMaxRequestCount|TestAuthRuntimeMaintenanceHook_DisablesCodexAuthFileAfterQuotaProbeUnauthorized|TestAuthRuntimeMaintenanceHook_DisablesAuthFileOnCodexUsageLimitReached' -v` 通过
  - 本轮正式验收结论为 `accepted`
- 备注:
  - manual disable 不误解锁由 `TestAuthMaintenanceRecoveryCandidateForAuth_RequiresSystemMarker` 覆盖
  - `go test ./sdk/cliproxy/... ./internal/api/handlers/management/...` 仍暴露既有 patch-fields 失败，但不落在本轮 disabled recovery 调用链内

### R4.5. 在当前 worktree 合并远端主线并处理 worktree

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R4 已完成且验收结论为 `accepted` / `conditionally accepted`
  - 已确认本地主工作区保持未触碰
  - 当前 worktree `devone/*` 分支已提交完毕，并可在当前 worktree 内执行 `fetch` / `merge` / `push`
  - 用户明确确认允许执行固定 closeout / 清理动作
- 产出:
  - 已记录本轮固定 closeout 路径：fetch 远端主线 -> merge 到当前 worktree -> push `devone/*` 分支 -> push 当前 HEAD 到远端主线
  - 远端 `devone/*` 分支已保留
  - 目标主线已完成 merge+push，或阻塞原因已登记
  - worktree 已按结论清理或保留待后续处理
- 验证:
  - 若已完成 closeout：`worktree-merge --confirm` 执行成功
  - 若清理目录：`worktree-remove --confirm` 执行成功
  - 若暂未完成 closeout：任务包与台账已写明阻塞原因与下一次继续路径
- 备注:
  - 当前主工作区本身处于 dirty 且分叉状态，只能作为旁路参考，不作为开发或收尾现场
  - 当前 worktree 已在本地完成 merge，并已把 `devone/20260409-120036-disabled-auth-auto-refresh-recovery` 与 `HEAD:CLIProxyAPIPlus-gdtiti` 推送到 `gdtiti`
  - 用户已把 closeout 目标分支改为 `gdtiti/CLIProxyAPIPlus-gdtiti`
  - `git ls-remote --heads gdtiti CLIProxyAPIPlus-gdtiti` 已确认远端目标分支存在
  - `worktree-merge --into CLIProxyAPIPlus-gdtiti --remote gdtiti --confirm` 已成功；cleanup 仍等待用户决定是否执行 `worktree-remove --confirm`

## 可选任务

### O1. 提供恢复日志查询接口

- 类型: 可选任务
- 状态: skipped_optional
- 前置条件:
  - 必备恢复链路已稳定
  - 用户要求管理端可查询恢复日志
- 产出:
  - 管理端查询/下载 recovery log 的接口
- 验证:
  - 能按时间或 auth 维度读取日志
- 备注:
  - 本轮 required-only 不推进

### O2. 扩展到更多 provider 或数据库化审计

- 类型: 可选任务
- 状态: skipped_optional
- 前置条件:
  - `codex` 路径已稳定
  - 用户明确要求扩展
- 产出:
  - 其他 provider 支持或新的审计存储
- 验证:
  - 不影响当前 `codex` 逻辑
- 备注:
  - 本轮不新增 PG/SQLite 事件中心

## 当前波次

- 当前波次: completion-wave-1
- 当前聚焦任务:
  - worktree cleanup 决策
- 不在本波次的任务:
  - 无

## 变更记录

- [20260409-120036] 初始化任务文档包，执行模式为 `required-only`
- [20260409-当前轮] 完成 discovery 收敛；锁定推荐方案为“auth-maintenance disabled recovery + JSONL 持久化日志”；执行基线固定避开当前本地主工作区 `main`
- [20260409-131406] worktree=created；目录=.devone/worktree/20260409-120036-disabled-auth-auto-refresh-recovery；分支=devone/20260409-120036-disabled-auth-auto-refresh-recovery；端口=35983；R2.5->done
- [20260409-当前轮] R3->done；disabled codex auth auto-recovery、recent buffer、JSONL 日志与 runtime hook marker 已落地；定向测试通过；全包仍有既有失败待 acceptance 决策
- [20260409-当前轮] R4->done；acceptance audit ready=true；fresh recovery / runtime-hook 定向测试通过；验收结论=accepted；R4.5 仍待用户确认后续 mergeback
- [20260409-当前轮] 进入 devone-end；completion audit ready=true；目标分支修正为 `gdtiti/CLIProxyAPIPlus-gdtiti`；当前 worktree 已提交并处于 `ahead 1`；`worktree-merge --into CLIProxyAPIPlus-gdtiti --remote gdtiti` dry-run 成功，R4.5 当前待用户确认真实 merge
- [20260409-180709] worktree-merge=done；remote=gdtiti；into=CLIProxyAPIPlus-gdtiti；branch=devone/20260409-120036-disabled-auth-auto-refresh-recovery
- [20260409-当前轮] R4.5->done；当前 worktree 内已完成 fixed closeout，远端 `gdtiti/devone/20260409-120036-disabled-auth-auto-refresh-recovery` 与 `gdtiti/CLIProxyAPIPlus-gdtiti` 已同步到提交 `efc65e16`；cleanup 状态为 `waiting_user_confirmation`
