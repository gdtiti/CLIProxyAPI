# 相关知识

## 文档元信息

- 任务名称: ghcr-only-main-publish
- 任务目录: .devone/data/20260406-224921-ghcr-only-main-publish
- 创建时间: 20260406-224921
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: execution
- 当前状态: in_progress

## 已知事实

- 现有实现: `.github/workflows/docker-image.yml` 目前有 `docker_amd64` / `docker_arm64` / `docker_manifest` 三个 job，都使用 DockerHub 登录与 DockerHub 标签命名。
- 相关文档: `.github/workflows/docker-branch.yml` 已经使用 GHCR、`docker/metadata-action` 和单 job multi-arch 发布。
- fresh evidence: 远端 run `24037097490` 中 `Login to GitHub Container Registry` 已成功，说明正式发布 workflow 的 registry/login 侧问题已经修掉。
- 模块事实: 当前 `go.mod` 的模块名为 `github.com/router-for-me/CLIProxyAPI/v6`，因此 `internal/authfs` 应该来自当前仓库源码，而不是外部拉取旧版 module。
- 当前阻塞: `Build and push` 失败日志证明正式镜像构建仍会在 Docker 内执行 `go build`，且当前分支此前缺失认证回收站相关源码。

## 术语与定义

- 

## 调研结论

- 结论 1: 主线 `docker-image` 手动触发 run `24035942823` 的直接失败点是 `Login to DockerHub`，不是代码编译；这一点已通过 GHCR-only 改造解决。
- 结论 2: 复用 `docker-branch.yml` 的 GHCR 结构可以最小化改动风险，并一次性移除 DockerHub secrets、临时架构标签与 cleanup 逻辑。
- 结论 3: run `24037097490` 说明真实阻塞已切换为源码缺失：`internal/authfs` 包与认证回收站 handler 方法不完整，必须在当前仓库补齐，而不是再改 registry 配置。

## 交接差异矩阵

- 产品事实源: 用户要求 GHCR-only。
- 知识事实源: 仓库已有 GHCR workflow 样式可复用。
- 实现事实源: 正式发布 workflow 仍绑定 DockerHub。
- 8 维摘要: 目标明确、实现点单一、依赖清晰、风险集中在 CI YAML、无需改业务代码、无需改 Dockerfile、需独立 worktree、验证以静态核对和远端 workflow 为主。

## 风险假设

- 假设: GitHub Container Registry 只要给 job 配置 `packages: write` 并用 `secrets.GITHUB_TOKEN` 登录即可满足当前发布需要。
- 验证方式: run `24037097490` 已证明 GHCR 登录成功；下一步只需验证补丁推送后的 `Build and push`。

## 知识升级候选

- 可升级为正式 wiki 的知识:
  - 正式发布 workflow 由 DockerHub 迁移至 GHCR 的固定模式与验证步骤
  - 当 GHCR 登录已成功时，应继续看 `Build and push` 日志，不要把旧的 DockerHub 根因误当作当前问题
- 仍停留在任务级草稿的知识:
  - run `24035942823` 与 `24037097490` 这两次具体失败记录
  - 本轮补回认证回收站源码的文件级细节
- 升级前缺口: 需要补丁推送后的最新 workflow 成功证据。

## 待补证据

- 改造后 `docker-image.yml` 的 diff 与静态检验结果。
- run `24037097490` 的 `Build and push` 失败日志归档。
- 认证回收站补丁推送后的手动 dispatch 或后续 tag run 成功证据。
