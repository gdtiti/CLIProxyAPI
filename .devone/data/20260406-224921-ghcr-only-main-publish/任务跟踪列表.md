# 任务跟踪列表

## 文档元信息

- 任务名称: ghcr-only-main-publish
- 任务目录: .devone/data/20260406-224921-ghcr-only-main-publish
- 创建时间: 20260406-224921
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- Worktree 目录: .devone/worktree/20260406-224921-ghcr-only-main-publish
- Worktree 分支: devone/20260406-224921-ghcr-only-main-publish
- 开发端口: 37206
- 当前阶段: execution
- 当前状态: in_progress

## 执行策略

- 当前选择:
  - `full`: 全量执行并跟踪必备任务与可选任务
  - `required-only`: 只执行必备任务；可选任务保留记录但默认不推进
- 本次实际模式: required-only
- 工作流日志: `.devone/wiki/logs/devone-workflow-log.md`（只用于人工检查，不作为平时知识读取入口）
- 结构化回写提示:
  - 更新阶段、波次、聚焦任务时，优先使用 `update-status`
  - 更新单个任务块的类型、状态、前置条件、产出、验证或备注时，优先使用 `update-task-block`
  - 模式切换时，优先使用 `sync-mode`

## 状态枚举

- `pending`: 尚未开始
- `in_progress`: 正在执行
- `blocked`: 有明确阻塞
- `done`: 已完成且已有证据
- `deferred`: 已确认延期
- `skipped_optional`: 因执行模式或用户选择而跳过的可选任务
- `rejected`: 经验收失败

## 任务包状态枚举

- `in_progress`: 当前任务包仍在推进
- `blocked`: 当前任务包存在明确阻塞
- `done`: 实现已完成，待正式验收
- `accepted`: 验收通过
- `conditionally_accepted`: 有条件通过
- `rejected`: 验收未通过

## 必备任务

### R1. 锁定问题定义与执行模式

- 类型: 必备任务
- 状态: done
- 前置条件: 已确认项目根目录
- 产出:
  - `需求说明.md` 的问题定义、目标、范围
  - 记录执行模式
- 验证: 需求说明与任务跟踪列表已同步
- 备注:

### R2. 形成可批准设计与阶段计划

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R1 已完成
  - 已收集最小必要上下文
- 产出:
  - `设计书.md`
  - `单元测试设计文档.md` 初稿
- 验证: 存在清晰方案、风险、阶段计划和测试设计
- 备注:

### R2.5. 创建 worktree 分支与开发端口

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R2 已完成
  - 设计已批准
- 产出:
  - `.devone/worktree/<任务id>/` 目录可用
  - 独立 git 分支已创建/已绑定到 worktree
  - 端口已分配并写回文档元信息
- 验证:
  - `worktree-status` 显示 worktree 已创建且状态清晰
  - `Worktree 目录 / Worktree 分支 / 开发端口` 已写入 `## 文档元信息`
- 备注:

### R3. 完成最小可验证实现

- 类型: 必备任务
- 状态: done
- 前置条件:
  - 设计已批准
  - 关键依赖已确认
- 产出:
  - 代码改动
  - `技术说明.md`
- 验证: 至少一条目标验证通过
- 备注:

### R4. 完成验收门禁核对

- 类型: 必备任务
- 状态: in_progress
- 前置条件:
  - R3 已完成
  - 本轮 fresh evidence 已收集
- 产出:
  - 需求映射
  - 门禁结论
- 验证:
  - GHCR 登录链路已由远端 workflow fresh evidence 证明可用
  - 需在推送当前源码修复后补最新 `Build and push` 结果，才能给出 `accepted` / `conditionally accepted` / `rejected`
- 备注: `24037097490` 已证明 DockerHub 问题消失，但暴露出认证回收站源码缺失导致的 `go build` 失败；当前已在 worktree 补齐缺失实现并完成本地编译验证，待推送后补最新远端 run。

### R4.5. 按主工作区状态回合并并处理 worktree

- 类型: 必备任务
- 状态: pending
- 前置条件:
  - R4 已完成且验收结论为 `accepted` / `conditionally accepted`
  - 已完成主工作区状态检查，并确认主工作区只执行 git 集成操作
  - worktree `devone/*` 分支已提交完毕并可推送远端
  - 用户明确确认允许执行固定 closeout / 清理动作
- 产出:
  - 已记录本轮固定 closeout 路径：push `devone/*` 分支 -> 从远端主线签出并同步 -> merge -> push 主线
  - 远端 `devone/*` 分支已保留
  - 目标主线已完成 merge+push，或阻塞原因已登记
  - worktree 已按结论清理或保留待后续处理
- 验证:
  - 若已完成 closeout：`worktree-merge --confirm` 执行成功
  - 若清理目录：`worktree-remove --confirm` 执行成功
  - 若暂未完成 closeout：任务包与台账已写明阻塞原因与下一次继续路径
- 备注:

## 可选任务

### O1. 扩展优化与非关键增强

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 必备任务主链稳定
  - 用户允许扩展范围
- 产出: 增强项或体验优化
- 验证: 不影响必备链路
- 备注:

### O2. 全量回归或更深测试覆盖

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 核心修复已通过
  - 测试环境可用
- 产出:
  - 更广回归结果
  - 补充测试用例
- 验证: 回归结果已记录到 `技术说明.md` / `单元测试设计文档.md`
- 备注:

## 当前波次

- 当前波次: Wave 2
- 当前聚焦任务: R4 - 提交认证回收站补丁并补最新 GHCR workflow 证据
- 不在本波次的任务: R4.5、O1、O2

## 变更记录

- [20260406-224921] 初始化任务文档包，执行模式为 `required-only`
- [20260406-230000] 已创建 worktree `.devone/worktree/20260406-224921-ghcr-only-main-publish`，分支 `devone/20260406-224921-ghcr-only-main-publish`，开发端口 `37206`
- [20260406-231000] 已完成 `docker-image.yml` 的 GHCR-only 改造，并通过 `DOCKERHUB` 零命中与 YAML 解析校验
- [20260406-232500] 远端 run `24037097490` 已证明 GHCR 登录成功；同次 run 暴露 `internal/authfs` 与认证回收站处理函数缺失，已在当前 worktree 补齐本地源码并通过 `go build -buildvcs=false ./cmd/server`
