# 单元测试设计文档

## 文档元信息

- 任务名称: ghcr-only-main-publish
- 任务目录: .devone/data/20260406-224921-ghcr-only-main-publish
- 创建时间: 20260406-224921
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 测试目标

- 需要被保护的核心行为: `docker-image` workflow 能在不依赖 DockerHub secrets 的前提下，为正式发布场景生成 GHCR 多架构镜像标签。
- 这轮必须覆盖的失败模式: 残留 `DOCKERHUB_*` 变量；缺失 `packages: write`；YAML 结构错误；丢失 `latest` / 版本标签之一。

## 单元测试设计

### UT-001

- 目标: 静态检查 `docker-image.yml` 中的登录与发布目标是否完全切换到 GHCR。
- 覆盖函数/模块: `.github/workflows/docker-image.yml`
- 输入: workflow 文本内容
- 预期输出: 仅出现 `ghcr.io`、`github.actor`、`secrets.GITHUB_TOKEN`、`packages: write` 等 GHCR 配置，不再出现 `DOCKERHUB_`。
- 失败表现: 仍有 DockerHub env/secrets/login/cleanup 片段。

## 属性测试设计

### PT-001

- 性质: 在 `workflow_dispatch` 与 `push tags v*` 两类触发下，workflow 的镜像标签生成逻辑保持稳定。
- 输入空间: `github.ref_name = main`、`github.ref_name = vX.Y.Z`
- 不变量: 都会生成 `latest` 与 `${VERSION}` 两个标签，并对 `linux/amd64` + `linux/arm64` 同时发布。
- 预期失败模式: 只生成单架构、缺少版本标签或缺少 latest 标签。

## 测试夹具与数据策略

- 夹具: 当前仓库中的 workflow YAML 文本。
- 真实数据策略: 使用真实文件 diff、真实 `git grep` / YAML 解析结果；若触发远端 workflow，则基于真实 GitHub Actions run 结果。
- 禁止使用的假路径: 不伪造 workflow 成功，不用手写“理论通过”代替静态/远端证据。

## 命令与执行计划

- 单元测试命令: `git diff -- .github/workflows/docker-image.yml`、`git grep -n "DOCKERHUB" -- .github/workflows/docker-image.yml`、`python - <<'PY' ... yaml.safe_load(...)`
- 属性测试命令: 推送后必要时手动 `workflow_dispatch` 一次 `docker-image`
- 最大单次测试时长: 60s

## 需求映射

- [REQ-001] -> UT-001
- [REQ-002] -> PT-001

## 实际执行结果

- 已运行:
  - `git grep -n "DOCKERHUB" -- .github/workflows/docker-image.yml`：确认正式 workflow 已无 DockerHub 残留
  - `python -c "import pathlib,yaml; yaml.safe_load(...)"`：确认 workflow YAML 可解析
  - 远端 GitHub Actions run `24037097490`：确认 GHCR 登录成功，但 `Build and push` 暴露新的源码缺失
  - `go build -buildvcs=false ./cmd/server`：确认补齐认证回收站源码后当前 worktree 可编译
  - 远端 GitHub Actions run `24039577894`：确认补丁推送后 `docker_image` job 全部成功
- 未运行:
  - 更广的业务回归测试（本轮仅处理 CI 构建阻塞）
- 差异说明: 本轮验证重点已完成从“是否还依赖 DockerHub”到“当前分支源码能否通过正式镜像构建中的 `go build`”的闭环；当前未完成项只剩 closeout 级别的主工作区卫生与合并动作，不属于功能验证缺口。
