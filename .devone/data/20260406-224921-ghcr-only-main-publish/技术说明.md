# 技术说明

## 文档元信息

- 任务名称: ghcr-only-main-publish
- 任务目录: .devone/data/20260406-224921-ghcr-only-main-publish
- 创建时间: 20260406-224921
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: execution
- 当前状态: in_progress

## 实际改动摘要

- 本轮改动: 将正式镜像 workflow 从 DockerHub 三 job 发布链路改成单 job 的 GHCR multi-arch 发布链路，并为本任务补齐最小 DevOne 文档与 worktree 台账。
- 涉及文件/模块:
  - `.github/workflows/docker-image.yml`
  - `.devone/data/20260406-224921-ghcr-only-main-publish/*`
  - `.devone/wiki/projects/devone-worktree-ledger.md`
- 未改动但已核对:
  - `.github/workflows/docker-branch.yml`（作为 GHCR 参考实现）
  - 2026-04-06 失败 run `24035942823`（确认根因是 DockerHub 登录失败）
- 结构化回写提示:
  - 更新固定章节时，优先使用 `update-doc-section`
  - 更新 `## Wave 执行记录` 下的单个 `### Wave N` 时，优先使用 `append-wave-record`
  - 更新 `## 并行批次记录` 下的单个 `### Batch N` 时，优先使用 `append-parallel-batch`

## Wave 执行记录

### Wave 1

- 目标: 在独立 worktree 中把 `docker-image.yml` 改为 GHCR-only，并补齐执行前文档证据。
- 改动:
  - 创建 `devone/20260406-224921-ghcr-only-main-publish` worktree，记录路径、分支、端口。
  - 删除 `DOCKERHUB_REPO`、DockerHub 登录、arm64/amd64 临时标签、manifest 合成与 DockerHub cleanup。
  - 新增 `REGISTRY=ghcr.io`、`IMAGE_NAME=${{ github.repository }}`、`packages: write`、GHCR 登录、`docker/metadata-action` 和单次 multi-arch `build-push-action`。
  - 补写需求、设计、相关知识、任务跟踪、测试设计和 worktree 台账。
- 结果: 本地静态检查已通过；待提交推送并根据需要补远端 workflow fresh evidence。

## 并行批次记录

- 使用提示:
  - 当前 wave 若使用子代理并行，优先把边界、写集、验证目标与汇总结论落到这里
  - 单个批次卡片优先通过 `append-parallel-batch` 回写

### Batch 1

- batch_type: none
- task_ids: none
- write_set: none
- verify_target: none
- forbidden_actions: none
- single_writer: main agent
- findings: 本轮未启用子代理并行
- results: 串行完成

## 验证记录

- 命令: `git grep -n "DOCKERHUB" -- .github/workflows/docker-image.yml`
  结果: 无命中，返回 `NO_DOCKERHUB_REFS`
  说明: 确认正式 workflow 已移除 DockerHub 变量与清理逻辑

- 命令: `python -c "import pathlib,yaml; yaml.safe_load(pathlib.Path('.github/workflows/docker-image.yml').read_text(encoding='utf-8'))"`
  结果: 解析成功，job 键为 `docker_image`
  说明: 基本 YAML 结构有效

- 命令: `git diff -- .github/workflows/docker-image.yml`
  结果: 变更符合预期，核心 diff 为 DockerHub -> GHCR 与三 job -> 单 job multi-arch
  说明: 仍需在提交前复核最终 staged diff

## 风险、阻塞与未验证项

- 风险: 仓库其它文档/compose 默认镜像仍引用 DockerHub，本轮未处理。
- 阻塞: 无
- 未验证项: 推送后的 GitHub Actions fresh evidence 尚未补采；尚未确认是否立即 merge 回 `gdtiti/main`。

## 验收准备

- 可交给 `devone-acceptance` 的证据:
  - 失败 run `24035942823` 的根因记录
  - `docker-image.yml` GHCR-only diff
  - `DOCKERHUB` 零命中检查
  - YAML 解析通过结果
- 仍需补充的证据:
  - 提交 SHA 与远端分支
  - 如需更强验收，再补一次远端 workflow dispatch 结果
