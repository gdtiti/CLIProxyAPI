# 需求说明

## 文档元信息

- 任务名称: ghcr-only-main-publish
- 任务目录: .devone/data/20260406-224921-ghcr-only-main-publish
- 创建时间: 20260406-224921
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 问题定义

- 当前问题: `.github/workflows/docker-image.yml` 仍依赖 `DOCKERHUB_USERNAME` / `DOCKERHUB_TOKEN` / `DOCKERHUB_REPO`，主线手动触发 `docker-image` 时在 `Login to DockerHub` 步骤失败，镜像无法发布。
- 影响对象: `gdtiti/main` 上的正式镜像发布流程、依赖主线镜像的部署与拉取链路。
- 触发背景: 2026-04-06 手动触发 `docker-image` workflow（run `24035942823`）后，`docker_amd64` 与 `docker_arm64` job 都在 DockerHub 登录步骤失败，`docker_manifest` 被跳过。
- 为什么现在处理: 用户已明确要求“不要用 DockerHub，只需要提交到 GitHub 的 docker 仓库”，当前真实阻塞点也正是 DockerHub 登录失败。

## 目标与成功标准

- 目标 1: 将 `docker-image` workflow 改为仅登录并发布到 GitHub Container Registry（`ghcr.io`）。
- 目标 2: 保留现有 `workflow_dispatch` + `push tags v*` 触发语义，并继续产出可拉取的多架构镜像标签。

## 范围边界

### 本次必须完成

- 修改 `.github/workflows/docker-image.yml`，去除 DockerHub secrets / login / cleanup 依赖。
- 在独立 worktree 中完成实现、静态核对、提交并推送到 `gdtiti` 远端分支。
- 把本次问题定义、设计、验证计划和执行记录沉淀到当前任务包。

### 本次明确不做

- 不处理仓库内其它文档、`docker-compose.yml` 或历史说明里残留的 DockerHub 文案。
- 不变更 `docker-image` 的触发条件，不额外扩展 main 分支自动发布策略。
- 不在本轮直接修改业务代码或 Dockerfile。

## 验收真相陈述

- [REQ-001] `docker-image.yml` 中不再出现 `DOCKERHUB_USERNAME`、`DOCKERHUB_TOKEN`、`DOCKERHUB_REPO`，镜像登录与发布只使用 GHCR + `secrets.GITHUB_TOKEN`。
- [REQ-002] `docker-image.yml` 仍可在 `workflow_dispatch` 与 `v*` tag push 下产出 `linux/amd64` + `linux/arm64` 多架构镜像，并生成 `latest` 与版本标签。

## 约束与依赖

- 技术约束: 必须遵守 DevOne；实际文件改动只能发生在独立 worktree；尽量复用仓库现有 GHCR 工作流模式。
- 业务约束: 用户明确要求不要再使用 DockerHub，只接受 GitHub Container Registry。
- 外部依赖: GitHub Actions、`docker/login-action@v3`、`docker/metadata-action@v5`、`docker/build-push-action@v6`、`secrets.GITHUB_TOKEN` 的 packages:write 权限。

## 风险与待确认项

- 风险: 仓库中仍有其它 DockerHub 文案与默认镜像引用，本轮不改；若后续拉取侧仍默认指向 DockerHub，需要另开任务处理。
- 待确认: 本轮先完成 GHCR-only workflow 改造并推送分支；是否继续合并回 `gdtiti/main`，待本轮验证与用户后续指令决定。

## 模式说明

- 若执行模式为 `required-only`，本文件中的目标和范围仍需完整记录，但实施与跟踪只强制推进必备任务
