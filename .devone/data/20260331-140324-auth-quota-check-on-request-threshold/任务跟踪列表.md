# 任务跟踪列表

## 文档元信息

- 任务名称: auth quota check on request threshold
- 任务目录: .devone/data/20260331-140324-auth-quota-check-on-request-threshold
- 创建时间: 20260331-140324
- 工作流档位: 极简 (devone-mini)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- Worktree 目录:
- Worktree 分支:
- 开发端口:
- 当前阶段: acceptance
- 当前状态: conditionally_accepted

## 执行策略

- 当前选择:
  - `full`: 全量执行并跟踪必备任务与可选任务
  - `required-only`: 只执行必备任务；可选任务保留记录但默认不推进
- 本次实际模式: required-only
- 工作流日志: `.devone/wiki/logs/devone-workflow-log.md`（只用于人工检查，不作为平时知识读取入口）
- 结构化回写提示:
  - 更新阶段、波次、聚焦任务时，优先使用 `update-status`
  - 更新单个任务块的类型、状态、前置条件、产出、验证或备注时，优先使用 `update-task-block`
  - 模式切换时，优先使用 `sync-mode`

## 状态枚举

- `pending`: 尚未开始
- `in_progress`: 正在执行
- `blocked`: 有明确阻塞
- `done`: 已完成且已有证据
- `deferred`: 已确认延期
- `skipped_optional`: 因执行模式或用户选择而跳过的可选任务
- `rejected`: 经验收失败

## 任务包状态枚举

- `in_progress`: 当前任务包仍在推进
- `blocked`: 当前任务包存在明确阻塞
- `done`: 实现已完成，待正式验收
- `accepted`: 验收通过
- `conditionally_accepted`: 有条件通过
- `rejected`: 验收未通过

## 必备任务

### R1. 锁定问题定义与执行模式

- 类型: 必备任务
- 状态: done
- 前置条件: 已确认项目根目录
- 产出:
  - `需求说明.md` 的问题定义、目标、范围
  - 记录执行模式
- 验证: 需求说明与任务跟踪列表已同步
- 备注:
  - 已按默认假设 `devone-mini + required-only + classic + single` 推进，并在任务包中记录。

### R2. 形成可批准设计与阶段计划

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R1 已完成
  - 已收集最小必要上下文
- 产出:
  - `设计书.md`
  - `单元测试设计文档.md` 初稿
- 验证: 存在清晰方案、风险、阶段计划和测试设计
- 备注:
  - docs-first 已结合 runtime auth wiki 和 2026-03-31 两个相关任务包完成。

### R2.5. 创建 worktree 分支与开发端口

- 类型: 必备任务
- 状态: skipped_optional
- 前置条件:
  - R2 已完成
  - 设计已批准
- 产出:
  - `.devone/worktree/<任务id>/` 目录可用
  - 独立 git 分支已创建/已绑定到 worktree
  - 端口已分配并写回文档元信息
- 验证:
  - `worktree-status` 显示 worktree 已创建且状态清晰
  - `Worktree 目录 / Worktree 分支 / 开发端口` 已写入 `## 文档元信息`
- 备注:
  - 本轮在主工作区直接完成最小范围改动，未创建 worktree。

### R3. 完成最小可验证实现

- 类型: 必备任务
- 状态: done
- 前置条件:
  - 设计已批准
  - 关键依赖已确认
- 产出:
  - 代码改动
  - `技术说明.md`
- 验证: 至少一条目标验证通过
- 备注:
  - 已完成配置接线、统一的 Codex 请求维护入口、quota probe 逻辑、定向测试、示例配置和 wiki 同步。

### R4. 完成验收门禁核对

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R3 已完成
  - 本轮 fresh evidence 已收集
- 产出:
  - 需求映射
  - 门禁结论
- 验证: 可以给出 `accepted` / `conditionally accepted` / `rejected`
- 备注:
  - `REQ-001` 和 `REQ-002` 都有 fresh evidence；targeted tests 通过，但 `go test ./internal/api/handlers/management -count=1` 仍受既有失败测试 `TestListAuthFiles_ExposesQuotaWindowFieldsAfterExecutionFailure` 影响，因此本轮记为 `conditionally_accepted`。

### R4.5. 合并回主线并清理 worktree

- 类型: 必备任务
- 状态: pending
- 前置条件:
  - R4 已完成且验收结论为 `accepted` / `conditionally accepted`
  - 用户明确确认允许合并与清理
- 产出:
  - worktree 分支已合并回目标主线分支
  - worktree 目录已移除
  - （可选）worktree 分支已删除
- 验证:
  - `worktree-merge --confirm` 执行成功
  - `worktree-remove --confirm` 执行成功
- 备注:

## 可选任务

### O1. 扩展优化与非关键增强

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 必备任务主链稳定
  - 用户允许扩展范围
- 产出: 增强项或体验优化
- 验证: 不影响必备链路
- 备注:

### O2. 全量回归或更深测试覆盖

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 核心修复已通过
  - 测试环境可用
- 产出:
  - 更广回归结果
  - 补充测试用例
- 验证: 回归结果已记录到 `技术说明.md` / `单元测试设计文档.md`
- 备注:

## 当前波次

- 当前波次: Wave 1
- 当前聚焦任务:
  - R4. 整理验收证据并准备提交推送
- 不在本波次的任务:
  - O1
  - O2

## 变更记录

- [20260331-140324] 初始化任务文档包，执行模式为 `required-only`
- [20260331-1412] 完成 docs-first discovery，锁定在 management runtime hook 上扩展 Codex quota probe 间隔删除能力
- [20260331-143242] 完成实现、wiki 回写和 fresh evidence 收集；management 全包仍有既有失败测试，本轮状态记为 `conditionally_accepted`
