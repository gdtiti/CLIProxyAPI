# 技术说明

## 文档元信息

- 任务名称: disable-auth-on-upstream-status-codes
- 任务目录: .devone/data/20260331-134029-disable-auth-on-upstream-status-codes
- 创建时间: 20260331-134029
- 工作流档位: 极简 (devone-mini)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: conditionally_accepted

## 实际改动摘要

- 本轮改动:
  - 为 `auth-maintenance` 新增 `disable-status-codes` 配置项。
  - 在 `sdk/cliproxy/service_auth_maintenance.go` 增加状态码触发 disable candidate 逻辑，并固定 disable 优先于 delete。
  - 调整 `disableAuthMaintenanceCandidate`，写回真实 disable reason，不再写死 `usage_limit_reached`。
  - 在 management runtime hook 同步支持 `disable-status-codes`。
  - 补充配置测试、service 测试、management runtime hook 测试。
  - 更新 `config.example.yaml` 与运行态维护 wiki。
- 涉及文件/模块:
  - `internal/config/config.go`
  - `internal/config/auth_maintenance_config_test.go`
  - `sdk/cliproxy/service_auth_maintenance.go`
  - `sdk/cliproxy/service_auth_maintenance_test.go`
  - `internal/api/handlers/management/auth_files_runtime_hook.go`
  - `internal/api/handlers/management/auth_files_runtime_hook_test.go`
  - `config.example.yaml`
  - `.devone/wiki/projects/runtime-auth-maintenance.md`
- 未改动但已核对:
  - `internal/api/handlers/management/handler.go`
  - `.devone/wiki/patterns/auth-maintenance-runtime-pattern.md`
- 结构化回写提示:
  - 更新固定章节时，优先使用 `update-doc-section`
  - 更新 `## Wave 执行记录` 下的单个 `### Wave N` 时，优先使用 `append-wave-record`
  - 更新 `## 并行批次记录` 下的单个 `### Batch N` 时，优先使用 `append-parallel-batch`

## Wave 执行记录

### Wave 1

- 目标:
  - 实现 `disable-status-codes` 并拿到最小可验证证据。
- 改动:
  - 配置结构新增字段
  - service 层新增状态码禁用判断与优先级
  - management runtime hook 同步支持状态码禁用
  - 测试与文档补齐
- 结果:
  - 目标达成；存在一个与本轮无关的 management 包既有失败测试，故本轮结论为 `conditionally_accepted`

## 并行批次记录

- 使用提示:
  - 当前 wave 若使用子代理并行，优先把边界、写集、验证目标与汇总结论落到这里
  - 单个批次卡片优先通过 `append-parallel-batch` 回写

### Batch 1

- batch_type:
  - local-sequential
- task_ids:
  - R3
  - R4
- write_set:
  - `internal/config/*`
  - `sdk/cliproxy/*`
  - `internal/api/handlers/management/*`
  - `config.example.yaml`
  - `.devone/wiki/projects/runtime-auth-maintenance.md`
- verify_target:
  - 配置加载
  - service 状态码禁用
  - management runtime hook 状态码禁用
- forbidden_actions:
  - 不修改无关 wiki
  - 不回滚既有脏改动
- single_writer:
  - true
- findings:
  - service 层已有通用 disable action，新增状态码触发条件即可复用
  - management hook 原先只支持 `usage_limit_reached` 特殊禁用，需同步才能保证语义一致
- results:
  - 代码、测试和文档均已落地

## 验证记录

- 命令:
  - `go test ./internal/config -count=1`
  - `go test ./sdk/cliproxy -count=1`
  - `go test ./internal/api/handlers/management -count=1`
  - `go test ./internal/api/handlers/management -run AuthRuntimeMaintenanceHook -count=1`
- 结果:
  - `internal/config` 通过
  - `sdk/cliproxy` 通过
  - `internal/api/handlers/management -count=1` 失败，失败用例为既有测试 `TestListAuthFiles_ExposesQuotaWindowFieldsAfterExecutionFailure`
  - `internal/api/handlers/management -run AuthRuntimeMaintenanceHook -count=1` 通过
- 说明:
  - 本轮 fresh evidence 以 targeted tests 为准；全包失败未由本轮改动引入

## 风险、阻塞与未验证项

- 风险:
  - 若用户把同一状态码同时配置到 `disable-status-codes` 和 `delete-status-codes`，当前实现会优先禁用，需要文档明确该优先级。
- 阻塞:
  - 无
- 未验证项:
  - 未修复 management 包既有失败测试，因此未拿到该包全量通过结果

## 验收准备

- 可交给 `devone-acceptance` 的证据:
  - 需求、设计、测试设计与技术说明已回写
  - `go test ./internal/config -count=1` 通过
  - `go test ./sdk/cliproxy -count=1` 通过
  - `go test ./internal/api/handlers/management -run AuthRuntimeMaintenanceHook -count=1` 通过
  - `config.example.yaml` 与运行态维护 wiki 已同步
- 仍需补充的证据:
  - 若要求整包全量通过，需要另行处理既有失败测试 `TestListAuthFiles_ExposesQuotaWindowFieldsAfterExecutionFailure`
