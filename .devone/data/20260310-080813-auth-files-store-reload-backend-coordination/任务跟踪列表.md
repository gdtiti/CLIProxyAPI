# 任务跟踪列表

## 文档元信息

- 任务名称: auth-files-store-reload-backend-coordination
- 任务目录: .devone/data/20260310-080813-auth-files-store-reload-backend-coordination
- 创建时间: 20260310-080813
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: workflow
- 当前状态: in_progress

## 执行策略

- 当前选择:
  - `full`: 全量执行并跟踪必备任务与可选任务
  - `required-only`: 只执行必备任务；可选任务保留记录但默认不推进
- 本次实际模式: required-only

## 状态枚举

- `pending`: 尚未开始
- `in_progress`: 正在执行
- `blocked`: 有明确阻塞
- `done`: 已完成且已有证据
- `deferred`: 已确认延期
- `skipped_optional`: 因执行模式或用户选择而跳过的可选任务
- `rejected`: 经验收失败

## 任务包状态枚举

- `in_progress`: 当前任务包仍在推进
- `blocked`: 当前任务包存在明确阻塞
- `done`: 实现已完成，待正式验收
- `accepted`: 验收通过
- `conditionally_accepted`: 有条件通过
- `rejected`: 验收未通过

## 必备任务

### R1. 锁定问题定义与执行模式

- 类型: 必备任务
- 状态: done
- 前置条件: 已确认项目根目录
- 产出:
  - `需求说明.md` 的问题定义、目标、范围
  - 记录执行模式
- 验证: 需求说明与任务跟踪列表已同步
- 备注:
  已将“上传 auth file 未写 store，导致 PG 与其它节点仍旧数据”写入任务定义，
  并保留 `required-only` 执行模式。

### R2. 形成可批准设计与阶段计划

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R1 已完成
  - 已收集最小必要上下文
- 产出:
  - `设计书.md`
  - `单元测试设计文档.md` 初稿
- 验证: 存在清晰方案、风险、阶段计划和测试设计
- 备注:
  已锁定“上传时同步 `PersistAuthFiles` + 当前节点 reload + 失败恢复原文件”的设
  计，不把范围扩展到跨节点自动广播刷新。

### R3. 完成最小可验证实现

- 类型: 必备任务
- 状态: done
- 前置条件:
  - 设计已批准
  - 关键依赖已确认
- 产出:
  - 代码改动
  - `技术说明.md`
- 验证: 至少一条目标验证通过
- 备注:
  已完成 `UploadAuthFile` 修复与定向测试，且 `go test ./internal/api/handlers/management`
  和 `go build ./cmd/server` 已通过。

### R4. 完成验收门禁核对

- 类型: 必备任务
- 状态: in_progress
- 前置条件:
  - R3 已完成
  - 本轮 fresh evidence 已收集
- 产出:
  - 需求映射
  - 门禁结论
- 验证: 可以给出 `accepted` / `conditionally accepted` / `rejected`
- 备注:
  fresh evidence 已收集，待本轮收口时给出最终门禁结论。

## 可选任务

### O1. 扩展优化与非关键增强

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 必备任务主链稳定
  - 用户允许扩展范围
- 产出: 增强项或体验优化
- 验证: 不影响必备链路
- 备注:

### O2. 全量回归或更深测试覆盖

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 核心修复已通过
  - 测试环境可用
- 产出:
  - 更广回归结果
  - 补充测试用例
- 验证: 回归结果已记录到 `技术说明.md` / `单元测试设计文档.md`
- 备注:

## 当前波次

- 当前波次:
  Wave 1: 修复上传 auth file 未写 store 的后端同步链路
- 当前聚焦任务:
  R4 验收门禁核对
- 不在本波次的任务:
  其它节点自动广播/自动轮询 reload 机制

## 变更记录

- [20260310-080813] 初始化任务文档包，执行模式为 `required-only`
- [20260310-162500] 追加上传 auth file 未写 store 的问题定义、设计与测试证据；
  R1-R3 完成，R4 进入验收门禁核对
