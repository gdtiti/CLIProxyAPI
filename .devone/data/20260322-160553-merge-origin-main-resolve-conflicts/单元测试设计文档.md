# 单元测试设计文档

## 文档元信息

- 任务名称: merge-origin-main-resolve-conflicts
- 任务目录: .devone/data/20260322-160553-merge-origin-main-resolve-conflicts
- 创建时间: 20260322-160553
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: completion
- 当前状态: accepted

## 测试目标

- 需要被保护的核心行为:
  在解决真实 merge 冲突后，仍然保留双方新增能力，并保证 translator、watcher、auth conductor 的关键行为不回退。
- 这轮必须覆盖的失败模式:
  合并时丢失 `parameters` 默认 schema、丢失 `item_reference` 剥离、丢失 builtin tool alias 归一化、丢失图片富内容转换、回退到 watcher 的旧扫描实现，或削弱 conductor 的 429 / model-support 状态处理。

## 单元测试设计

### UT-001

- 目标:
  验证冲突文件是否全部从未合并状态切换为已解决状态。
- 覆盖函数/模块:
  git 未合并索引状态。
- 输入:
  `git diff --name-only --diff-filter=U`
- 预期输出:
  返回空结果。
- 失败表现:
  任一冲突文件仍出现在未合并列表中。

### UT-002

- 目标:
  验证 `translator` 相关冲突在保留双方逻辑后仍可通过现有测试。
- 覆盖函数/模块:
  `codex/openai/chat-completions`
  `codex/openai/responses`
  `openai/openai/responses`
- 输入:
  `go test ./internal/translator/codex/openai/chat-completions ./internal/translator/codex/openai/responses ./internal/translator/openai/openai/responses`
- 预期输出:
  相关包测试通过，且无编译错误。
- 失败表现:
  JSON 构造路径、工具归一化或富内容兼容逻辑回退。

### UT-003

- 目标:
  验证 `watcher` 与 `sdk/cliproxy/auth` 的状态/缓存逻辑在合并后仍可通过目标包测试。
- 覆盖函数/模块:
  `internal/watcher`
  `sdk/cliproxy/auth`
- 输入:
  `go test ./internal/watcher`
  `go test ./sdk/cliproxy/auth`
- 预期输出:
  两个目标包测试通过。
- 失败表现:
  缓存重建、配额冷却、Codex 429 临时禁用或 model state 行为回退。

## 属性测试设计

### PT-001

- 性质:
  冲突解决完成后，未合并索引必须清零，且目标包验证结果应与实际修改范围一致。
- 输入空间:
  当前 merge 后工作区与 6 个冲突文件。
- 不变量:
  不再存在 `UU` 文件，且通过的测试包都直接覆盖当前冲突涉及模块。
- 预期失败模式:
  冲突清零但行为回退，或因无关现存测试问题误判本轮修改失败。

## 测试夹具与数据策略

- 夹具:
  当前仓库真实 merge 现场与既有测试集。
- 真实数据策略:
  只用仓库真实冲突文件与现有包测试，不伪造冲突输入。
- 禁止使用的假路径:
  删除一侧逻辑后“让测试先过”、手工伪造不存在的上游上下文、静默跳过失败包并宣称整体验证完成。

## 命令与执行计划

- 单元测试命令:
  `git diff --name-only --diff-filter=U`
  `go test ./internal/translator/codex/openai/chat-completions ./internal/translator/codex/openai/responses ./internal/translator/openai/openai/responses`
  `go test ./internal/watcher`
  `go test ./sdk/cliproxy/auth`
- 属性测试命令:
  `git status --short --untracked-files=all`
- 最大单次测试时长: 60s

## 需求映射

- [REQ-001] -> `git diff --name-only --diff-filter=U`
- [REQ-002] -> `go test ./internal/translator/codex/openai/chat-completions ./internal/translator/codex/openai/responses ./internal/translator/openai/openai/responses`
- [REQ-003] -> `go test ./internal/watcher`
- [REQ-004] -> `go test ./sdk/cliproxy/auth`

## 实际执行结果

- 已运行:
  `git diff --name-only --diff-filter=U`
  `go test ./internal/translator/codex/openai/chat-completions ./internal/translator/codex/openai/responses ./internal/translator/openai/openai/responses`
  `go test ./internal/watcher`
  `go test ./sdk/cliproxy/auth`
  `go test ./internal/runtime/executor`
- 未运行:
  整仓测试
- 差异说明:
  `internal/runtime/executor` 已实际执行，但被与本轮冲突无关的现存 `github_copilot_executor_test.go` 编译错误阻塞；因此本轮验收只把通过的目标包测试作为直接证据，不把该无关失败误判为冲突解决回退。
