# 需求说明

## 文档元信息

- 任务名称: merge-origin-main-resolve-conflicts
- 任务目录: .devone/data/20260322-160553-merge-origin-main-resolve-conflicts
- 创建时间: 20260322-160553
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: completion
- 当前状态: accepted

## 问题定义

- 当前问题:
  用户在拉取最新 git 后进入真实 merge 冲突现场，需要对与 `origin/main` 合并产生的冲突逐块分析并解决，同时尽量保留双方功能完整性。
- 影响对象:
  当前开发分支 `CLIProxyAPIPlus-gdtiti` 的 merge 现场、6 个未合并文件，以及这些文件对应的 translator、watcher、auth conductor 与测试行为。
- 触发背景:
  2026-03-22 的历史核对曾得到“`origin/main` 已被当前分支包含、无冲突可解”的结论；2026-03-24 用户再次同步最新 git 后，仓库出现新的真实冲突现场，需要按当前事实重新处理。
- 为什么现在处理:
  未合并文件会阻塞后续编译、验证和继续开发，且若错误偏向单侧实现，容易造成功能回退。

## 目标与成功标准

- 目标 1:
  基于当前真实 merge 现场，识别并清空所有未合并文件。
- 目标 2:
  逐块合并冲突时尽量保留双方新增能力，若两边实现存在新旧差异，则优先采用更新且更完整的实现路径。
- 目标 3:
  用目标包验证证明本轮冲突解决未破坏直接受影响模块，并把与本轮无直接关系的现存问题隔离记录。

## 范围边界

### 本次必须完成

- 识别真实未合并文件并逐块完成冲突解决
- 记录每个冲突文件的合并取舍
- 运行与当前冲突文件直接相关的目标包验证
- 回写 DevOne 任务包

### 本次明确不做

- 不把与当前冲突无关的整仓现存问题伪装成本轮回归
- 不在未获确认时直接执行回主线 merge、删除分支或清理目录
- 不为了“先过测试”而删掉任一侧新增逻辑

## 验收真相陈述

- [REQ-001] 已基于真实 merge 现场解决全部未合并文件，`git diff --name-only --diff-filter=U` 返回空。
- [REQ-002] 6 个冲突文件均按“尽量保留双方能力、优先更新实现”的原则完成逐块合并，并已写回索引。
- [REQ-003] 已完成与当前改动直接相关的目标包验证；与本轮无直接关系的现存失败已被单独记录，不混入本轮冲突结论。

## 约束与依赖

- 技术约束:
  必须使用当前主工作区的真实 merge 现场，不能凭历史任务包或聊天上下文替代当前证据。
- 业务约束:
  冲突处理必须优先保留功能与兼容性，且不能为了简化合并而丢弃较新的实现路径。
- 外部依赖:
  当前仓库的 merge 索引状态、现有单元测试、以及 `go test` 对目标包的可执行环境。

## 风险与待确认项

- 风险:
  当前主工作区仍有大量非冲突 merge 改动，若未经确认直接尝试回主线 merge，容易把超出本任务范围的改动一并带入。
- 待确认:
  是否继续做高风险的回主线 merge / 清理动作，需要用户在收尾阶段单独确认。

## 模式说明

- 若执行模式为 `required-only`，本文件中的目标和范围仍需完整记录，但实施与跟踪只强制推进必备任务
