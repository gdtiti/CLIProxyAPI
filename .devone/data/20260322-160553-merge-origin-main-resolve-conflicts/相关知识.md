# 相关知识

## 文档元信息

- 任务名称: merge-origin-main-resolve-conflicts
- 任务目录: .devone/data/20260322-160553-merge-origin-main-resolve-conflicts
- 创建时间: 20260322-160553
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: completion
- 当前状态: accepted

## 已知事实

- 现有实现:
  2026-03-22 的历史证据显示，当时当前分支已包含一次与 `origin/main` 的合并提交 `93e460c8`，正式执行 `git merge origin/main` 返回 `Already up to date.`。
- 相关文档:
  本任务包在 2026-03-24 已从历史 `accepted` 结论恢复到推进态，并补记了新的真实冲突解决过程。
- 历史约束:
  必须基于当前 fresh git 现场判断是否存在冲突，不能因历史结论或用户预期而编造或忽略冲突分析。
- 当前现场:
  2026-03-24 出现 6 个真实未合并文件，现已全部解决并写回索引；`git diff --name-only --diff-filter=U` 返回空。
- 当前仓库状态:
  主工作区仍有大量非冲突 merge 改动，因此不能默认执行回主线 merge；当前任务包也没有有效 worktree。

## 术语与定义

- `origin/main`: 上游主线远端跟踪分支
- `merge-base`: 当前分支与 `origin/main` 的共同祖先
- `Already up to date.`: merge 无新增上游提交可并入的直接证据
- `merge readiness`: 收尾阶段对“是否能安全回主线 merge”的结论，不等于“冲突解决已验收”

## 调研结论

- 结论 1:
  历史结论“2026-03-22 无冲突”仍然成立，但它只适用于当时现场，不能覆盖 2026-03-24 的新 merge 现场。
- 结论 2:
  当前真实冲突解决后，`translator`、`watcher`、`sdk/cliproxy/auth` 目标包验证均已通过，说明直接受影响模块没有出现明显回退。
- 结论 3:
  `go test ./internal/runtime/executor` 的失败来自现存 `github_copilot_executor_test.go` 编译问题，需与本轮冲突解决结论隔离记录。
- 结论 4:
  `.devone/wiki/` 中没有与本任务直接相关的长期知识条目；本轮知识闭环以任务包内文档为准，无需额外补 wiki。

## 关键合并取舍

- `usage_helpers_test.go` 同时保留 Codex 配额解析测试与 usage latency 测试。
- `codex_openai_request.go` 统一采用 `[]byte`/`SetRawBytes` 路径，同时保留 `parameters` 为空时的默认 schema。
- `codex_openai-responses_request.go` 同时保留 `item_reference` 剥离与 builtin tool alias 归一化。
- `openai_openai-responses_request.go` 以 `[]byte` JSON 构造为主线，同时保留富文本与 `input_image` 转换。
- `watcher/clients.go` 保留 `rebuildFileAuthCaches` 新抽象，删除旧重复扫描逻辑。
- `sdk/cliproxy/auth/conductor.go` 合并 `resultModel` 语义、QuotaWindow 传播、Codex 429 临时禁用与 model-support 检测。

## 风险与恢复线索

- 风险:
  主工作区脏状态会阻塞 `worktree-merge --confirm`；这属于收尾阶段阻塞，不代表本轮冲突解决失败。
- 主工作区路径事实:
  从提交图看，`main` 是当前 `HEAD` 的祖先，后续若改走主工作区提交/切分支路径，理论上更接近 fast-forward 合入，而不是再次人工解冲突。
- 恢复线索:
  当前任务包的 acceptance gate 已通过；completion gate 只依赖任务状态与收尾对账是否同步。
- 清理结论:
  当前没有 worktree 可清理；如需主线 merge，必须先由用户明确确认高风险操作。

## 待补证据

- 无 acceptance 级缺口；后续仅需在 closeout 结论中明确 merge readiness 与 cleanup 状态。
