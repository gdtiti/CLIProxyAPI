# 技术说明

## 文档元信息

- 任务名称: 配置暂停分布式记录与认证回收站清理能力
- 任务目录: .devone/data/20260402-095726-配置暂停分布式记录与认证回收站清理能力
- 创建时间: 20260402-095726
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: accepted

## 实际改动摘要

- 本轮改动:
  - 已为 `distributed-sync` 落地 `recording-enabled` 配置项，并把该开关接入 `PostgresStore.recordDistributedChangeTx` 前 gate；`cmd/server/main.go` 与 `internal/api/server.go` 已接上初始化与配置热更新 setter。
  - 已完成 auth recycle bin Wave 1 闭环：新增 `internal/authfs/recycle_bin.go` 统一回收站路径/恢复映射能力，management 单删/批删/清空、runtime hook 自动删除、maintenance 删除链均改为软删除到 `auth-dir/.trash/`。
  - 已完成 recycle bin 对外接线：`internal/api/server.go` 已注册 `GET /auth-files/recycle-bin`、`POST /auth-files/recycle-bin/restore`、`DELETE /auth-files/recycle-bin`。
  - 已补齐活动链 `.trash` 过滤：watcher、store、sdk/auth `List()`、maintenance path resolve 均忽略 `.trash`，避免回收站文件被重新当成活动 auth。
  - 已完成 Wave 2 distributed cleanup：新增 `internal/api/handlers/management/distributed_sync.go`，在 `internal/api/server.go` 注册 `DELETE /distributed-sync/outbox` 与 `DELETE /distributed-sync/versions`，并在 `internal/store/distributed_sync.go` 落地 `DeleteDistributedOutbox(ctx, before *time.Time)`、`DeleteDistributedVersions(ctx, resources ...string)`。
  - 已完成 Wave 2 usage cleanup / usage-history 配置收敛：`internal/config/config.go` 新增 `UsageHistoryConfig` 与默认值，`config.example.yaml` 暴露 `usage-history` 配置；`cmd/server/main.go` 的 `CleanupWorker` 改为优先读取 YAML 配置，同时保留 `USAGE_RETAIN_DAYS / USAGE_AGG_RETAIN_DAYS` env override。
  - `internal/api/handlers/management/usage.go` 已支持 PG / 内存双模式清理；`DELETE /usage` 既可全删 reset，也可基于 `before=YYYY-MM-DD` 做 prune；对应内存态由 `RequestStatistics.Reset/PruneBefore`、`LoggerPlugin.ResetUsageState/PruneUsageStateBefore`、`HotBuffer.Reset/PruneBefore`、`WriteQueue.Reset` 收敛。
  - 验收前已补强 REQ-001 的直接自动化证据：`internal/store/distributed_sync_test.go` 新增 `TestRecordDistributedChangeTxSkipsWhenRecordingDisabled`，直接覆盖 `SetDistributedRecordingEnabled(false)` 时 `recordDistributedChangeTx` 不再写 `sync_versions` / `sync_outbox` 的行为。
  - 已补齐 Wave 2 对应测试，并于 20260402-125719 重新执行 fresh targeted `go test` 与 `go build ./cmd/server`，结果全绿。
  - closeout 收敛补充（20260402-165022）：继续只在 worktree 内收敛 auth recycle bin 删除链的防御分支；management runtime hook 新增“路径已在 `.trash` 中时只禁用、不重复搬运”的快速返回，service maintenance 删除链新增 `authDir==""` / path 已在 `.trash` 时仅删 token record 的兜底，并在 `os.Rename(...)->os.ErrNotExist` 时回退到 token record 清理。
- 涉及文件/模块:
  - 当前已改:
    - `internal/authfs/recycle_bin.go`
    - `internal/config/config.go`
    - `internal/config/usage_history_config_test.go`
    - `config.example.yaml`
    - `internal/store/distributed_sync.go`
    - `internal/store/distributed_sync_test.go`
    - `internal/store/postgresstore.go`
    - `internal/store/objectstore.go`
    - `internal/store/gitstore.go`
    - `internal/api/handlers/management/auth_files.go`
    - `internal/api/handlers/management/auth_files_runtime_hook.go`
    - `internal/api/handlers/management/distributed_sync.go`
    - `internal/api/handlers/management/usage.go`
    - `internal/api/handlers/management/usage_cleanup_test.go`
    - `internal/api/handlers/management/auth_files_delete_test.go`
    - `internal/api/handlers/management/auth_files_batch_test.go`
    - `internal/api/handlers/management/auth_files_runtime_hook_test.go`
    - `internal/api/server.go`
    - `internal/watcher/auth_cache_reload.go`
    - `internal/watcher/clients.go`
    - `internal/watcher/events.go`
    - `internal/usage/hot_buffer.go`
    - `internal/usage/hot_buffer_test.go`
    - `internal/usage/logger_plugin.go`
    - `internal/usage/logger_plugin_test.go`
    - `internal/usage/write_queue.go`
    - `internal/usage/write_queue_test.go`
    - `sdk/auth/filestore.go`
    - `sdk/auth/filestore_test.go`
    - `sdk/cliproxy/service_auth_maintenance.go`
    - `sdk/cliproxy/service_auth_maintenance_test.go`
    - `cmd/server/main.go`
  - 未改动但已核对:
    - `internal/distributedsync/manager.go`
    - `internal/store/postgres_reload.go`
  - `.devone/wiki/projects/runtime-auth-maintenance.md`
  - 历史 distributed-sync 与 auth-maintenance DevOne 任务包
- 关键证据链:
  - distributed recording 主写链仍然位于 `PostgresStore.PersistConfig / PersistAuthFiles / Save / Delete -> recordDistributedChangeTx -> sync_versions + sync_outbox`，对应实现集中在 `internal/store/postgresstore.go` 与 `internal/store/distributed_sync.go`；当前 gate 已只接在 `recordDistributedChangeTx` 前，没有改变 `IsConfigured()` 生命周期语义。
  - recycle bin 共用能力已沉淀到 `internal/authfs/recycle_bin.go`，management/runtime/maintenance 三条删除链统一复用；management API 已具备 list / restore / purge 外部入口。
  - `.trash` 过滤已落在 `listAuthFilesFromDisk`、`buildAuthFileEntry`、`reloadAuthFile`、`PostgresStore.List`、`sdk/auth/filestore.go:List`、`internal/store/objectstore.go:List`、`internal/store/gitstore.go:List`、`internal/watcher/auth_cache_reload.go`、`internal/watcher/clients.go`、`sdk/cliproxy/service_auth_maintenance.go:resolveAuthFilePath`，当前回收站文件不会重新进入活动视图。
  - distributed cleanup 已拆成两个显式管理动作：`DELETE /distributed-sync/outbox` 走 `DeleteDistributedOutbox`，支持 `before=YYYY-MM-DD`；`DELETE /distributed-sync/versions` 走 `DeleteDistributedVersions`，支持 `resource=config,auth` 或全量 reset，不把 outbox 清理与 version 重置混成一个接口。
  - usage cleanup 已同时覆盖 PG 与内存态：持久化模式会先删 `usage_details/usage_aggregates`，再 reset / prune `RequestStatistics`、`HotBuffer`、`WriteQueue`；纯内存模式不再报 “PG mode not enabled”，而是直接清理内存统计，避免“接口成功但 snapshot 残留”的伪收敛。
  - `usage-history` 的 YAML 默认值已经接入启动路径，当前 retention 可经配置文件控制，环境变量只做显式 override；用户 5 个目标对应的最小代码闭环已全部落地。
- 结构化回写提示:
  - 更新固定章节时，优先使用 `update-doc-section`
  - 更新 `## Wave 执行记录` 下的单个 `### Wave N` 时，优先使用 `append-wave-record`
  - 更新 `## 并行批次记录` 下的单个 `### Batch N` 时，优先使用 `append-parallel-batch`

## Wave 执行记录

### Wave 1

- 目标: 补齐 execution 证据链，确认独立 worktree，并把 Wave 1 最小写集/验证入口锁死后开始实现。
- 改动:
  - 已补 `需求说明.md`、`设计书.md`、`相关知识.md`、`单元测试设计文档.md`、`任务跟踪列表.md`。
  - 执行 execution gate：audit --target-phase execution，通过且阻塞 0。
  - 创建独立 worktree：.devone/worktree/20260402-095726-配置暂停分布式记录与认证回收站清理能力。
  - 绑定分支 devone/20260402-095726，并分配开发端口 31076。
  - 通过 update-status 将任务包切到 execution / Wave 1 / R3 in_progress，并同步文档元信息。
  - 在 worktree 内完成源码级证据链审计，锁定：
    1. `distributed-sync.recording-enabled` 只影响 `recordDistributedChangeTx` 调用前 gate，不改 `IsConfigured()` 生命周期语义。
    2. auth 回收站最小共享能力建议抽到 `internal/authfs/recycle_bin.go`，供 management/runtime/maintenance 三条删除链共用。
    3. distributed cleanup 路由需与现有 `DELETE /v0/management/usage` 分离，避免把高风险 `sync_versions` reset 与普通 outbox cleanup 混在同一接口。
    4. usage 清理必须同时覆盖 PG、`HotBuffer`、`defaultRequestStatistics`，否则会出现“接口成功但内存快照残留”的伪收敛。
  - 锁定 Wave 1 先行测试入口：`internal/store/distributed_sync_test.go`、`internal/api/handlers/management/auth_files_delete_test.go`、`internal/api/handlers/management/auth_files_batch_test.go`、`internal/api/handlers/management/auth_files_runtime_hook_test.go`、`sdk/cliproxy/service_auth_maintenance_test.go`。
  - 落地 `distributed-sync.recording-enabled`：
    - `internal/config/config.go` 新增字段。
    - `config.example.yaml` 新增示例配置。
    - `internal/store/distributed_sync.go` 新增 setter / getter，并在 `recordDistributedChangeTx` 前加 gate。
    - `cmd/server/main.go` / `internal/api/server.go` 在初始化和配置热更新时调用 `SetDistributedRecordingEnabled`。
  - 落地 management recycle bin 主链：
    - 新增 `internal/authfs/recycle_bin.go` 提供 `.trash` 路径规范化、恢复映射与目录修剪能力。
    - `internal/api/handlers/management/auth_files.go` 已将单删 / 批删 / all-delete 改为软删除，并新增回收站 list / restore / purge handler helper。
  - 补齐 Wave 1 闭环缺口：
    - `internal/api/server.go` 已注册 recycle bin 路由。
    - `internal/api/handlers/management/auth_files_runtime_hook.go` 的 runtime delete 已改为回收站软删除。
    - `sdk/cliproxy/service_auth_maintenance.go` 的 maintenance delete 已改为回收站软删除，并同步持久化 auth files。
    - `internal/watcher/*`、`internal/store/*`、`sdk/auth/filestore.go` 已补 `.trash` 过滤，避免回收站文件回流到活动集合。
    - `internal/api/handlers/management/*_test.go`、`sdk/cliproxy/service_auth_maintenance_test.go`、`sdk/auth/filestore_test.go` 已补对应测试。
- 结果:
  - discovery 范围与设计已锁定，execution 入口已核实，当前可以直接在 worktree 开始写代码。
  - R2.5 已完成，主工作区脏改动已与实施工作隔离。
  - 任务包当前已有“调用链 -> 已落地代码 -> 热更新点 -> 测试入口 -> fresh 验证结果”的闭环证据。
  - 当前 fresh regression 结果：
    - `go test ./internal/store/... ./internal/watcher/... ./internal/api/handlers/management/... ./sdk/auth/... ./sdk/cliproxy/...`
    - `go build ./cmd/server`
    - 结果：上述测试包全部通过，`cmd/server` 构建通过。
  - Wave 1 已闭环；acceptance 仍等待 R3 完成，下一步进入 Wave 2 的 distributed cleanup 与 usage cleanup。

### Wave 2

- 目标: 完成 distributed cleanup、usage cleanup 与 usage-history 配置化，把 5 个需求点全部收口到可验证实现。
- 改动:
  - 新增 `internal/api/handlers/management/distributed_sync.go`，提供 `DeleteDistributedSyncOutbox` / `DeleteDistributedSyncVersions`；在 `internal/api/server.go` 注册 `DELETE /distributed-sync/outbox`、`DELETE /distributed-sync/versions`。
  - `internal/store/distributed_sync.go` 新增 `DeleteDistributedOutbox(ctx, before *time.Time)`、`DeleteDistributedVersions(ctx, resources ...string)` 与 `normalizeDistributedResources()`；outbox 支持按 `created_at < before` 删除，versions 支持按 `config/auth` 精确 reset 或全量 reset。
  - `internal/config/config.go` 新增 `UsageHistoryConfig`、`DefaultUsageHistoryRetainDays`、`DefaultUsageHistoryAggregateRetainDays` 和 `Effective*` helper；`config.example.yaml` 暴露 `usage-history.retain-days / aggregate-retain-days`；`cmd/server/main.go` 的 `CleanupWorker` 改为优先读取 `cfg.UsageHistory.*`，再允许环境变量覆盖。
  - `internal/api/handlers/management/usage.go` 改为 PG / 内存双模式；`before=YYYY-MM-DD` 走 prune，全量删除走 reset。
  - `internal/usage/hot_buffer.go`、`internal/usage/logger_plugin.go`、`internal/usage/write_queue.go` 补齐 `Reset` / `PruneBefore` / `ResetUsageState` / `PruneUsageStateBefore`，保证 PG、HotBuffer、`defaultRequestStatistics` 和 pending queue 一起收敛。
  - 新增/补齐测试：
    - `internal/api/handlers/management/usage_cleanup_test.go`
    - `internal/config/usage_history_config_test.go`
    - `internal/store/distributed_sync_test.go`
    - `internal/usage/hot_buffer_test.go`
    - `internal/usage/logger_plugin_test.go`
    - `internal/usage/write_queue_test.go`
- 结果:
  - distributed cleanup 现在有显式管理入口，outbox 与 versions 不再混删。
  - usage retention 已从纯环境变量提升到 YAML 配置；手动清理后 PG、HotBuffer、`defaultRequestStatistics` 与 pending queue 可一致 reset / prune。
  - 20260402-125719 fresh 验证结果：
    - `go test ./internal/store/... ./internal/watcher/... ./internal/api/handlers/management/... ./internal/config ./internal/usage ./sdk/auth/... ./sdk/cliproxy/...`
    - `go build ./cmd/server`
    - 结果：上述测试包全部通过，`cmd/server` 构建通过。
  - R3 所需的 5 个需求点都已进入“代码 + targeted test + build”闭环；下一步进入 R4 验收门禁核对。

### Wave 3

- 补充说明:
  - 20260402-165022 在 closeout 阶段发现主工作区与 worktree 对 `auth_files_runtime_hook.go` / `service_auth_maintenance.go` 存在分叉后，继续仅在 worktree 内补做删除链防御性收敛，并维持 R4.5 阻塞状态不变。

- 目标: 做 R4 严格验收，补强 REQ-001 的直接测试证据，并补一轮冷启动/用户可观察烟测。
- 改动:
  - 在 `internal/store/distributed_sync_test.go` 新增 `TestRecordDistributedChangeTxSkipsWhenRecordingDisabled`，直接验证关闭 `recording-enabled` 后 `recordDistributedChangeTx` 返回 `nil` 且不会发出任何 DB 写入。
  - 重新执行 fresh targeted `go test ./internal/store/... ./internal/watcher/... ./internal/api/handlers/management/... ./internal/config ./internal/usage ./sdk/auth/... ./sdk/cliproxy/...` 与 `go build ./cmd/server`，确认补强后的最终代码状态仍为绿色。
  - 使用 worktree 分配端口 `31076` 做冷启动烟测：以临时配置启动 `server.exe`，并在本机用 `X-Management-Key` 访问管理路由。
  - 实际烟测结果：
    - `GET /v0/management/config` 返回 `200`，响应中可见 `usage-history.retain-days=30 / aggregate-retain-days=365` 与 `distributed-sync.recording-enabled=true`。
    - `DELETE /v0/management/distributed-sync/versions?resource=config` 在内存 store 场景返回 `400`，证明新路由已接线且失败可见，没有静默 fallback。
    - `DELETE /v0/management/usage` 在内存模式返回 `200 {"status":"ok"}`，证明新的无 PG 清理路径可达。
- 结果:
  - REQ-001 现在同时具备配置回显、调用链接线和直接单元测试三层证据。
  - 需求映射结论：
    - REQ-001 通过：`recording-enabled` 已接入启动/热更新与 `recordDistributedChangeTx` gate，且有直接单测保护。
    - REQ-002 通过：management / runtime / maintenance 删除链统一走 recycle bin。
    - REQ-003 通过：回收站 list / restore / purge 路由与 `.trash` 过滤链均已落地。
    - REQ-004 通过：distributed cleanup 已拆成 outbox 清理与 versions reset 两个显式动作，并有 handler/store 测试与失败可见性烟测。
    - REQ-005 通过：`usage-history` YAML 配置、PG/内存 reset+prune、以及内存模式手动清理路径均已落地并验证。
  - 新增管理路由在冷启动的本机服务上可直接访问，用户可观察行为与设计一致。
  - R4 的需求映射、真实数据/冷启动/失败可见性证据已经齐备，可进入最终验收结论与 completion gate。

### Wave 3 补充（R4.5 防御性收敛）

- 目标:
  - 在不扩大范围的前提下，收敛 auth recycle bin 删除链的已知边界：路径已在 `.trash`、`authDir` 为空、以及 `os.Rename` 命中 `os.ErrNotExist` 的竞态兜底。
- 改动:
  - `internal/api/handlers/management/auth_files_runtime_hook.go` 的 `removeManagedAuthFile` 新增 already-in-trash 快速返回；若 move 过程中命中 `errAuthFileNotFound`，继续回退为 `deleteTokenRecord + disableAuth`。
  - `sdk/cliproxy/service_auth_maintenance.go` 的 `deleteAuthMaintenanceCandidate` 新增两类防御分支：`authDir==""` 或 path 已在 `.trash` 时只删 token record；`os.Rename` 命中 `os.ErrNotExist` 时不再直接报错，而是回退到 token record 清理。
  - 新增 targeted tests：
    - `internal/api/handlers/management/auth_files_runtime_hook_test.go: TestRemoveManagedAuthFile_DisablesAuthAlreadyInTrash`
    - `sdk/cliproxy/service_auth_maintenance_test.go: TestDeleteAuthMaintenanceCandidate_DeletesTokenRecordWhenPathAlreadyInTrash`
    - `sdk/cliproxy/service_auth_maintenance_test.go: TestDeleteAuthMaintenanceCandidate_DeletesTokenRecordWhenAuthDirEmpty`
- 结果:
  - 20260402-165022 fresh targeted regression：
    - `go test ./internal/api/handlers/management ./sdk/cliproxy -run 'RemoveManagedAuthFile|DeleteAuthMaintenanceCandidate|AuthRuntimeMaintenanceHook|AuthMaintenance' -count=1`
    - `go test ./internal/api/handlers/management -run 'AuthRuntimeMaintenanceHook|DeleteAuthFile|AuthFiles' -count=1`
    - `go test ./sdk/cliproxy -run 'AuthMaintenance' -count=1`
  - `git add -u && git add internal/api/handlers/management/distributed_sync.go internal/api/handlers/management/usage_cleanup_test.go internal/authfs internal/config/usage_history_config_test.go && git commit -m "feat: add recycle bin and cleanup controls"`
  - `git -C J:\_Dev\_me\_cpa\CLIProxyAPIPlus rev-list --left-right --count main...devone/20260402-095726`
  - `git status --short --branch`
    - 结果：上述命令全部通过。
  - 本轮补充只收敛删除链边界，不改变既有 accepted 结论；R4.5 仍阻塞于“worktree 未提交入分支 + 主工作区 tracked dirty”。

## 并行批次记录

- 使用提示:
  - 当前 wave 若使用子代理并行，优先把边界、写集、验证目标与汇总结论落到这里
  - 单个批次卡片优先通过 `append-parallel-batch` 回写

### Batch 1

- batch_type:
- task_ids:
- write_set:
- verify_target:
- forbidden_actions:
- single_writer:
- findings:
- results:

## 验证记录

- 命令:
  - `rg -n "type DistributedSyncConfig|IsConfigured|type PostgresStore struct|PersistAuthFiles\\(|PersistConfig\\(|Save\\(|Delete\\(|recordDistributedChangeTx|DeleteAuthFile\\(|deleteAuthFileByName\\(|listAuthFilesFromDisk\\(|buildAuthFileEntry\\(|reloadAuthFile\\(|removeManagedAuthFile\\(|deleteAuthMaintenanceCandidate\\(|SetBaseDir\\(|DeleteUsageStatistics\\(|type RequestStatistics|defaultRequestStatistics|NewCleanupWorker\\(|setupDistributedSyncNotifier\\(|startDistributedSync\\(" -S internal cmd sdk config.example.yaml`
  - `Get-Content -Encoding utf8 internal/config/config.go`
  - `Get-Content -Encoding utf8 internal/store/postgresstore.go`
  - `Get-Content -Encoding utf8 internal/store/distributed_sync.go`
  - `go test ./internal/store/... ./internal/watcher/... ./internal/api/handlers/management/... ./sdk/auth/... ./sdk/cliproxy/...`
  - `go test ./internal/store/... ./internal/watcher/... ./internal/api/handlers/management/... ./internal/config ./internal/usage ./sdk/auth/... ./sdk/cliproxy/...`
  - `go build ./cmd/server`
  - `./server.exe -config ./.tmp-acceptance/config.yaml`
  - `Invoke-WebRequest -Headers @{ 'X-Management-Key' = 'acceptance-secret' } http://127.0.0.1:31076/v0/management/config`
  - `Invoke-WebRequest -Method Delete -Headers @{ 'X-Management-Key' = 'acceptance-secret' } http://127.0.0.1:31076/v0/management/distributed-sync/versions?resource=config`
  - `Invoke-WebRequest -Method Delete -Headers @{ 'X-Management-Key' = 'acceptance-secret' } http://127.0.0.1:31076/v0/management/usage`
  - `go test ./internal/api/handlers/management ./sdk/cliproxy -run 'RemoveManagedAuthFile|DeleteAuthMaintenanceCandidate|AuthRuntimeMaintenanceHook|AuthMaintenance' -count=1`
  - `go test ./internal/api/handlers/management -run 'AuthRuntimeMaintenanceHook|DeleteAuthFile|AuthFiles' -count=1`
  - `go test ./sdk/cliproxy -run 'AuthMaintenance' -count=1`
- 结果:
  - 已取得 execution 前 fresh code evidence，用于锁定 Wave 1 / Wave 2 最小写集、热更新点、现有删除链与测试入口。
  - 20260402-115419 在当前已落地改动上，`internal/store`、`internal/watcher`、`internal/api/handlers/management`、`sdk/auth`、`sdk/cliproxy`、`sdk/cliproxy/auth` 测试全部通过，`cmd/server` 构建通过，证明 Wave 1 diff 没有破坏相关包编译与既有 targeted 行为。
  - 20260402-125719 在补齐 Wave 2 后，`internal/store`、`internal/watcher`、`internal/api/handlers/management`、`internal/config`、`internal/usage`、`sdk/auth`、`sdk/cliproxy`、`sdk/cliproxy/auth` 测试全部通过，`cmd/server` 构建通过，证明 distributed cleanup、usage cleanup、usage-history 配置接线和新增 reset/prune 能力已经进入 fresh green 状态。
  - 20260402-132931 在补强 `TestRecordDistributedChangeTxSkipsWhenRecordingDisabled` 后，`internal/store`、`internal/watcher`、`internal/api/handlers/management`、`internal/config`、`internal/usage`、`sdk/auth`、`sdk/cliproxy`、`sdk/cliproxy/auth` 测试全部通过，`cmd/server` 构建通过。
  - 20260402-131535 ~ 20260402-131705 冷启动烟测成功：`server.exe` 在 `127.0.0.1:31076` 正常启动；`GET /v0/management/config` 返回 `200`；`DELETE /v0/management/distributed-sync/versions?resource=config` 返回 `400`；`DELETE /v0/management/usage` 返回 `200`。
  - 20260402-165022 追加删除链 defensive regression：management/runtime/service 新增 already-in-trash 与 `authDir` 为空兜底用例后，`go test ./internal/api/handlers/management ./sdk/cliproxy -run 'RemoveManagedAuthFile|DeleteAuthMaintenanceCandidate|AuthRuntimeMaintenanceHook|AuthMaintenance' -count=1`、`go test ./internal/api/handlers/management -run 'AuthRuntimeMaintenanceHook|DeleteAuthFile|AuthFiles' -count=1`、`go test ./sdk/cliproxy -run 'AuthMaintenance' -count=1` 全绿。
  - 20260402-170207 已将 worktree 源码改动提交到 `devone/20260402-095726`：commit=`7468707c feat: add recycle bin and cleanup controls`；当前 `main...devone/20260402-095726` 变为 `0 1`，worktree 仅剩 `.tmp-acceptance/`、`.tmp-go-build/` 两个未跟踪临时目录。
- 说明:
  - 当前验证证明 Wave 1 / Wave 2 都已闭环且编译稳定，并已补齐 REQ-001 的直接测试和冷启动烟测；剩余工作只剩 completion gate 与最终状态回写。

## 风险、阻塞与未验证项

- 风险:
  - 若 `recording-enabled` 误接到 `IsConfigured()`，会把 distributed sync 生命周期一并停掉，偏离需求。
  - 若 usage cleanup 只清 PG 或只清 `HotBuffer`，管理端会出现“清理成功但统计仍旧可见”的双状态残留。
  - 若 distributed cleanup 只清 outbox 不清 versions，或把两者混到一个不受控接口，会留下脏游标或把高风险 reset 暴露成普通操作。
- 阻塞:
  - 验收本身无新增阻塞；R4.5 closeout merge 已因主工作区脏改动被阻断，详见下方“阻塞补充”。
- 未验证项:
  - 非本轮 required-only 范围之外的整仓全量回归
  - `deleteAuthMaintenanceCandidate` 中 `os.Rename(...)->os.ErrNotExist` 的竞态兜底已实现，但本轮未单独构造稳定可重复的自动化复现；当前只验证了 already-in-trash / authDir empty 两个确定性边界。

- 阻塞补充:
  - 20260402-140913 已执行 completion gate 与 `worktree-merge --into main` dry-run；completion gate 为 `ready=true`，但 merge 被脚手架阻断，原因是项目根工作区仍有已修改 tracked 文件。
  - 当前已确认与本任务写集重叠的主工作区脏文件至少包括：`config.example.yaml`、`internal/api/server.go`、`internal/store/gitstore.go`、`internal/store/objectstore.go`、`sdk/auth/filestore.go`、`sdk/cliproxy/service_auth_maintenance.go`、`internal/api/handlers/management/auth_files_runtime_hook.go`。
  - 在主工作区清理前，不执行 `worktree-merge --confirm`，也不执行 worktree 删除，避免未合并改动丢失。

- 阻塞补充 2:
  - 进一步核对发现：`main...devone/20260402-095726` 的 `rev-list --left-right --count` 结果为 `0 0`，即当前 devone 分支与 `main` 没有任何 commit 差异。
  - 当前 worktree 里的本轮实现仍主要停留在未提交工作区：`git status --short --branch` 显示 28 个 tracked 修改、4 个未跟踪文件。
  - 这意味着即使先把主工作区清干净，若不先把 worktree 改动落到分支 HEAD，直接执行 `worktree-merge --confirm` 也只会合并现有分支提交，不会带上本轮实际代码。

- 阻塞补充 3:
  - 20260402-170207 已在 worktree 执行 `git commit -m "feat: add recycle bin and cleanup controls"`，提交哈希 `7468707c`。
  - 当前 `main...devone/20260402-095726` 的 `rev-list --left-right --count` 结果已变为 `0 1`，说明“分支与 main 0/0、改动未进入 HEAD”这条阻塞已经解除。
  - 当前剩余阻塞只剩主工作区 tracked dirty；在主工作区未清理前，merge dry-run 仍会被脚手架拦截。

- 阻塞补充 4:
  - 20260402-171444 再次执行 `audit --target-phase completion --workflow-profile devone --json`，结果仍为 `ready=true, blocking_issues=0`；completion gate 本身没有新增缺口。
  - 20260402-171444 执行 `worktree-status --json` 可见 worktree HEAD=`7468707cf60bc2e3a14d892367750f4119038df7`，源码已 clean，仅剩 `.tmp-acceptance/`、`.tmp-go-build/` 两个未跟踪临时目录。
  - 20260402-171444 执行 `worktree-merge --into main --json` 仍被脚手架直接拦截，报错为：`Project root worktree has modified tracked files; please commit or stash before merging`。
  - 已逐项核对主工作区 9 个 tracked dirty 文件与 worktree HEAD：没有任何一个文件内容完全相同。
    - `.devone/wiki/logs/devone-workflow-log.md`、`.devone/wiki/projects/devone-worktree-ledger.md` 仅在主工作区保留了 closeout 轨迹，尚未同步进 worktree 分支。
    - `config.example.yaml` 是本地环境化示例，包含环境路径、管理密钥哈希与 API key 等敏感/个性化内容，和本任务提交版示例配置不是同一语义，不能直接合并进主线。
    - `internal/api/server.go` 的主工作区 dirty 只补了 recycle bin 路由，缺少 worktree 已验收实现中的 `DELETE /distributed-sync/outbox`、`DELETE /distributed-sync/versions` 以及 `SetDistributedRecordingEnabled` 热更新接线。
    - `sdk/auth/filestore.go`、`sdk/cliproxy/service_auth_maintenance.go`、`internal/store/gitstore.go`、`internal/store/objectstore.go`、`internal/api/handlers/management/auth_files_runtime_hook.go` 虽然方向接近，但 helper 选型、条件合并和兜底细节与 worktree 提交版仍不一致；当前不能把主工作区 dirty 判定为“等价副本”。
  - 结论：当前 closeout 的唯一真实阻塞仍是主工作区 tracked dirty，但它不是“可忽略重复改动”，而是“需要用户明确决定如何保留/清理的独立本地偏移”。

## 验收准备

- 可交给 `devone-acceptance` 的证据:
  - Wave 1 fresh evidence：
    - auth recycle bin 路由 / runtime / maintenance / `.trash` 过滤代码改动
    - `go test ./internal/store/... ./internal/watcher/... ./internal/api/handlers/management/... ./sdk/auth/... ./sdk/cliproxy/...`
    - `go build ./cmd/server`
  - Wave 2 fresh evidence：
    - distributed cleanup 路由 / handler / store helper 代码改动
    - `usage-history` YAML 配置与 `CleanupWorker` 接线
    - `DELETE /usage` 的 PG / 内存 reset + prune 改动
    - `go test ./internal/store/... ./internal/watcher/... ./internal/api/handlers/management/... ./internal/config ./internal/usage ./sdk/auth/... ./sdk/cliproxy/...`
    - `go build ./cmd/server`
  - Wave 3 acceptance evidence：
    - `TestRecordDistributedChangeTxSkipsWhenRecordingDisabled`
    - worktree 冷启动烟测：`GET /v0/management/config = 200`、`DELETE /v0/management/distributed-sync/versions = 400`、`DELETE /v0/management/usage = 200`
    - `audit --target-phase acceptance --workflow-profile devone --json` => `ready=true`
    - `audit --target-phase completion --workflow-profile devone --json` => `ready=true`
- 当前结论:
  - 任务包已进入 `acceptance / accepted`。
  - 用户已确认 merge / cleanup；当前 worktree 改动已提交进分支 HEAD（`7468707c`），但 `worktree-merge --into main` 仍会被主工作区已修改 tracked 文件阻断；当前保留 worktree，待主工作区清理后重试。
