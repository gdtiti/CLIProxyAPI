# 需求说明
## 文档元信息
- 任务名称: workspace-and-previous-task-organization
- 任务目录: .devone/data/20260322-132550-workspace-and-previous-task-organization
- 创建时间: 20260322-132550
- 执行模式: 仅必备任务 (required-only)
- 当前阶段: acceptance
- 当前状态: conditionally_accepted

## 问题定义

- 当前问题:
  项目内已经存在 4 个 DevOne 历史任务包、2 个 worktree，以及主工作区未提交改动。
  这些状态没有被统一整理成一个可恢复入口，继续推进时容易选错任务包、误判代码归属，或在未确认前做危险清理。
- 影响对象:
  当前仓库维护者、下一轮继续执行的 Codex 会话、以及后续需要接手验收或合并的人。
- 触发背景:
  用户明确要求“整理工作区和之前的任务”，并显式指定使用 `devone`。
- 为什么现在处理:
  主工作区脏改动已经与 `20260318-172541-distributed-deployment-architecture-analysis` 高度重叠；
  如果不先整理，后续无论继续开发、验收还是清理 worktree，都会放大误操作风险。
- 任务形态:
  `single`。本轮目标是建立单一恢复入口，不做业务代码开发。

## 目标与成功标准
- 目标 1:
  明确列出历史任务包、对应阶段、worktree 状态和当前可继续入口。
- 目标 2:
  明确主工作区脏改动归属于哪个历史任务，并说明为什么。
- 目标 3:
  在不修改业务代码、不执行危险清理的前提下，形成新的 DevOne 整理任务包，作为后续恢复真值源。

## 范围边界

### 本次必须完成

- 盘点 `.devone/data/` 下历史任务包及其阶段状态。
- 盘点 `.devone/worktree/` 与 `git worktree list` 的实际映射关系。
- 盘点主工作区与活跃 worktree 的脏改动，并给出继续入口建议。
- 将上述事实写回当前整理任务包。

### 本次明确不做

- 不删除任何文件、目录、worktree 或分支。
- 不合并任何 worktree 分支回主线。
- 不修改业务代码、不补写历史任务实现。
- 不初始化 `.devone/wiki/`，仅记录其当前缺失状态。

## 验收真相陈述

- [REQ-001] 当前整理任务包必须记录历史任务包清单、阶段状态、worktree 对应关系和继续入口。
- [REQ-002] 当前整理任务包必须明确主工作区脏改动与 `20260318-172541-distributed-deployment-architecture-analysis` 的归属关系，并说明依据。
- [REQ-003] 本轮不得引入业务代码改动，也不得执行删除、合并、覆盖等危险操作。

## 约束与依赖
- 技术约束:
  事实以当前仓库文件、`.devone` 文档包、`git status`、`git worktree list` 和 worktree 内实际状态为准。
- 业务约束:
  历史任务不能被聊天记忆替代，必须回写到新的任务包作为恢复入口。
- 外部依赖:
  无外部网络或生产环境依赖；仅依赖本地 git 与现有文档包可读。

## 风险与待确认项
- 风险:
  `20260318-172541` 的未提交内容同时存在于主工作区与对应 worktree，后续若跨目录继续修改，容易造成证据分裂。
- 风险:
  `20260310-183634` 的 worktree 已干净，但任务包仍未正式进入 acceptance 结论，容易被误认为已完全结束。
- 待确认:
  若用户希望继续“物理清理”工作区，需要单独确认是否删除临时目录、移除 worktree、合并分支。

## 模式说明

- 若执行模式为 `required-only`，本文件中的目标和范围仍需完整记录，但实施与跟踪只强制推进必备任务。
