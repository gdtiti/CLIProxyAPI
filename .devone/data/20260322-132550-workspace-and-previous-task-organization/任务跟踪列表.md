# 任务跟踪列表

## 文档元信息
- 任务名称: workspace-and-previous-task-organization
- 任务目录: .devone/data/20260322-132550-workspace-and-previous-task-organization
- 创建时间: 20260322-132550
- 执行模式: 仅必备任务 (required-only)
- Worktree 目录:
- Worktree 分支:
- 开发端口:
- 当前阶段: acceptance
- 当前状态: accepted

## 执行策略

- 当前选择:
  - `full`: 全量执行并跟踪必备任务与可选任务
  - `required-only`: 只执行必备任务；可选任务保留记录但默认不推进
- 本次实际模式: required-only
- 结构化回写提示:
  - 当前任务为 docs-only 整理任务，不创建新 worktree
  - 涉及真实清理时，必须等待用户确认

## 状态枚举
- `pending`: 尚未开始
- `in_progress`: 正在执行
- `blocked`: 有明确阻塞
- `done`: 已完成且已有证据
- `deferred`: 已确认延期
- `skipped_optional`: 因执行模式或用户选择而跳过的可选任务
- `rejected`: 验收失败

## 任务包状态枚举
- `in_progress`: 当前任务包仍在推进
- `blocked`: 当前任务包存在明确阻塞
- `done`: 实现已完成，待正式验收
- `accepted`: 验收通过
- `conditionally_accepted`: 有条件通过
- `rejected`: 验收未通过

## 必备任务

### R1. 锁定问题定义与执行模式
- 类型: 必备任务
- 状态: done
- 前置条件:
  - 已确认项目根目录
- 产出:
  - `需求说明.md`
  - 当前任务包执行模式记录
- 验证:
  用户指令、`devone` 规则和当前任务包元信息已对齐。
- 备注:
  本轮按代理假设采用 `required-only`，因为任务目标是整理而不是开发。

### R2. 形成可批准设计与阶段计划

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R1 已完成
  - 已收集最小必要上下文
- 产出:
  - `设计书.md`
  - `单元测试设计文档.md`
- 验证:
  已明确本轮只做状态收口，不做危险清理和业务代码变更。
- 备注:
  推荐方案为“文档归档优先”的整理方案。

### R2.5. 判定是否需要新 worktree
- 类型: 必备任务
- 状态: done
- 前置条件:
  - R2 已完成
- 产出:
  - worktree 需求判定结论
- 验证:
  已确认本轮仅修改当前任务包文档，无业务代码写入，因此不创建新 worktree。
- 备注:
  现有两个历史 worktree 保持原样，不在本轮变动。

### R3. 完成最小可验证实现

- 类型: 必备任务
- 状态: done
- 前置条件:
  - 设计已锁定
  - 历史任务与 git 现场已盘点
- 产出:
  - 当前任务包 6 份文档
  - 整理结论与恢复入口
- 验证:
  通过 `git status`、`git worktree list`、任务包文档与 worktree 内状态完成交叉核对。
- 备注:
  已确认当前继续入口为 `20260318-172541-distributed-deployment-architecture-analysis`。
  已补充主工作区脏改动分类，可区分 watcher/auto-reload、distributed sync 与历史 DevOne 文档三组来源。

### R4. 完成验收门禁核对

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R3 已完成
  - fresh evidence 已收集
- 产出:
  - 需求映射
  - 门禁结论
- 验证:
  可给出 `conditionally_accepted` 结论。
- 备注:
  条件项不是当前整理失败，而是后续若要做真实清理，仍需用户确认。

### R4.5. 合并回主线并清理 worktree

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R4 已完成
  - 用户已明确要求：优先完成并提交整合到主工作区，而不是删除
- 产出:
  - 主工作区提交准备结论
  - “不删除、先收口” 的整理策略回写
- 验证:
  已确认 `20260318` 与 `20260322-141204` 的实现均已整合到主工作区并取得 fresh evidence；当前进入统一提交阶段
- 备注:
  本轮按修正后的用户要求执行提交整合，不执行 `worktree-remove` 等物理删除动作。

## 可选任务
### O1. 初始化 `.devone/wiki/` 入口

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 用户希望补齐 docs-first 入口
- 产出:
  - `.devone/wiki/` 初始索引
- 验证:
  wiki 可作为后续恢复入口被引用
- 备注:
  本轮只记录 wiki 缺失状态。

### O2. 为历史任务补正式 acceptance 结论
- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 用户选择继续收尾旧任务
- 产出:
  - `20260310-183634` 或 `20260318-172541` 的 acceptance 结论
- 验证:
  对应任务包状态被更新，且有 fresh evidence
- 备注:
  优先级低于当前整理动作本身。

## 当前波次

- 当前波次:
  Wave 1-3: 历史任务与工作区状态收口
- 当前聚焦任务:
  无
- 不在本波次的任务:
  真实清理、worktree 删除、分支合并、历史任务补测

## 变更记录

- [20260322-132550] 初始化任务文档包，执行模式为 `required-only`
- [20260322-133000] 已完成历史任务包、主工作区与 worktree 盘点，并确认继续入口为 `20260318-172541-distributed-deployment-architecture-analysis`
- [20260322-133000] 已确认 `20260310-183634-auth-files-bulk-upload-startup-performance` 为待收尾旧任务，worktree 当前干净
- [20260322-133000] 已确认本轮不执行删除、合并、worktree 移除等危险操作，任务包结论为 `conditionally_accepted`
- [20260322-154100] 已补充主工作区 dirty 分类清单，明确 watcher/auto-reload、distributed sync 与历史 DevOne 文档三组来源
- [20260322-151003] 已重新核对主工作区 `git status --short`、`git worktree list` 与当前任务包文档，确认整理结论未被后续操作破坏
- [20260322-154217] 用户明确要求：整理时先确认任务包是否已实施或在实施；对这类任务应优先完成并提交整合回主工作区，而不是删除
- [20260322-154504] 已确认 `20260318` 与 `20260322-141204` 的代码事实都已回写并收口到主工作区，当前整理焦点切换为提交而非删除
- [20260322-154505] 已确认 `devone/20260310-183634-auth-files-bulk-upload-startup-performance` 与 `devone/20260322-141204-arron196-design-alignment-config-auth-reload` 相对主工作区均无独有提交
- [20260322-154600] R4.5 标记为 done：本轮完成“确认任务包真实状态 -> 主工作区收口 -> fresh evidence -> 提交准备”，不再以删除 worktree 为完成条件
- [20260322-154601] 任务包状态更新为 `accepted`
